- Task: TLU-D-01 用真实 Codex lifecycle 事件验证 `working` 与 `done` 的自动切换
- Encountered Problem: 需要区分“人工 `set` 映射正确”和“真实 hook 事件链路真的能驱动 UI”是两件不同的事。
- Thought Process: 选择一条最简单的成功只读请求，先只验证真实对话是否能把 widget 从 `working` 自动带到 `done`。
- Options Considered: 继续依赖人工 `set`；用真实成功对话单独验证。
- Chosen Solution: 用真实只读 prompt 触发成功链路，记录 `run -> done`。
- Rationale: 这证明 UI 不只是本地调试可用，至少一条真实成功链路已经打通。
