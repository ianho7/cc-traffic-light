- Task: CCH-E-03 验证 global_summary 在 Claude/Codex 并存时行为合理
- Encountered Problem: 新增 Claude 记录后，全局 summary 可能被不正确的优先级规则污染。
- Thought Process: 对照 `global_summary`、`agents.claude.summary` 和 `agents.codex.summary`，确认 waiting 的 Codex 仍保持全局最高优先级。
- Options Considered: 只证明 Claude summary 更新；同时检查 global summary 没回归。
- Chosen Solution: 同时检查 global summary。
- Rationale: 这能证明 Claude 接入没有破坏现有总览逻辑。
