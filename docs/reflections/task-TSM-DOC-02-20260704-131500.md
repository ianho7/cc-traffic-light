- Task: TSM-DOC-02 更新构建命令说明，区分 Rust 宿主与 Tauri 前端
- Encountered Problem: 当前最危险的不是命令缺失，而是团队可能继续使用 `cargo build --workspace` 生成错误宿主。
- Thought Process: 我把前端 `pnpm build`、Rust 宿主 `cargo build -p taskbar-widget`、Tauri settings `cargo build -p taskbar-settings-tauri` 分开写，并明确各自用途。
- Options Considered: 只写一组最短命令；继续沿用 workspace 级 build 示例；按角色区分前端、settings、宿主的命令，并说明宿主为何必须单包构建。
- Chosen Solution: 在架构文档和 handoff 中统一写入推荐命令序列，并把 `cargo build --workspace --offline` 标成不适合宿主验收的路径。
- Rationale: 命令说明如果不连带约束一起写，文档会在下一次排障时再次失效。
