- Task: CSW-A-02 build taskbar_widget_hook.exe
- Encountered Problem: P0 不是只看源码通过，而是要求项目 hooks 能指向实际存在的二进制。
- Thought Process: 在 `cargo check` 通过后继续执行 `cargo build`，验证目标可执行文件能真正产出。
- Options Considered: 仅依赖历史构建产物；重新 `cargo build` 生成新二进制；先手改 hooks 再补构建。
- Chosen Solution: 先完成 `cargo build`，拿到当前源码对应的 `target/debug/taskbar_widget_hook.exe`。
- Rationale: `.codex/hooks.json` 将直接引用这个路径；如果不先构建，后续 trust 成功也可能因为目标文件缺失而完全不触发。
