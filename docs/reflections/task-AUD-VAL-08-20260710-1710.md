- Task: AUD-VAL-08 pack-all release artifact failure injection
- Encountered Problem: 原 pack-all 每次都先构建，无法安全地在不重复构建的情况下注入缺失 artifact 验证最终检查。
- Thought Process: 将 artifact 检查抽成 `-ValidateOnly` 入口，验证逻辑与正常打包末尾使用同一 `Show-Artifact` 和 hook version check。
- Options Considered: 删除 artifact 后完整重跑打包；只静态阅读脚本；增加只读验证模式并临时移动一个 artifact。
- Chosen Solution: 增加 `scripts/pack-all.ps1 -ValidateOnly`，临时移动 `taskbar-settings-tauri.exe` 后确认输出 `Tauri settings missing` 并恢复文件。
- Rationale: failure injection 可重复、不会触碰安装器或用户目录，也避免无意义地重新构建前端。
- Evidence: `pack-all.ps1 -ValidateOnly` success；missing-artifact run failed at `Show-Artifact` with `Tauri settings missing`；`artifact_restored=True`。
- Status: completed
