- Task: AUD-VAL-04 pnpm build
- Encountered Problem: pnpm 与 node 都解析到 C:\Program Files\Volta shim；执行版本或前端 build 时 Volta 尝试创建 C:\Users\admin\AppData\Local\Volta，但当前权限拒绝。
- Thought Process: 先尝试 pnpm 版本、直接运行 taskbar-settings-tauri/scripts/build.ps1，再定位 node/bun；TypeScript noEmit 可以通过，但完整 Vite build 仍依赖不可用的 node shim。
- Options Considered: 修改 Volta 目录权限；安装 Node；使用 Bun 代替 Node；记录环境阻塞。
- Chosen Solution: 不扩大权限或改工具链，记录为 blocked，并保留已通过的 npx TypeScript 检查。
- Rationale: 这是外部开发环境问题，不应通过修改项目或用户权限掩盖。
- Evidence: pnpm/node Volta error；npx tsc -p tsconfig.app.json --noEmit exit code 0；bun 读取 pnpm hardlink 受 EPERM。
- Status: blocked
