- Task: AUD-2-08 Codex lifecycle hook parse and trust preparation
- Encountered Problem: Codex `/hooks` reported `failed to parse hooks config ... expected value at line 1 column 1`, while PowerShell `ConvertFrom-Json` accepted the same file.
- Thought Process: Read the raw bytes of `%USERPROFILE%\.codex\hooks.json`; the first three bytes were `EF BB BF`, proving a UTF-8 BOM. Windows PowerShell 5.1 `Set-Content -Encoding UTF8` in the installer script was the source of the BOM.
- Options Considered: Manually edit the JSON without preserving the original; bypass Codex hook trust; change the parser; make the installer write UTF-8 without BOM and repair the current file with a backup.
- Chosen Solution: Added `Write-Utf8NoBom` to `install-codex-hooks.ps1`; repaired the current user file after creating `hooks.json.bom-backup-20260711-171257`.
- Rationale: Codex must parse the real hook configuration before review/trust; bypassing trust would invalidate the audit.
- Evidence: Raw bytes before repair `239 187 191 123`; after repair `123 13 10 ...`; JSON parse after repair passed; backup path recorded above.
- Status: completed
