- Task: CCH-B-03 触发真实 Notification shape-only sample
- Encountered Problem: 需要确认 Claude Code 的等待/提示类事件是否也携带 `session_id` 和事件名，而不是只有工具前后事件才有。
- Thought Process: 触发会产生通知的 Claude 行为，再读取 `.claude/hook-logs/Notification.jsonl` 中的真实 sample。
- Options Considered: 继续把等待态语义建立在推断上；先补真实 Notification 证据。
- Chosen Solution: 先补真实 Notification 证据。
- Rationale: 这样后续把 Notification 映射到 waiting 状态才有直接 payload 依据。
