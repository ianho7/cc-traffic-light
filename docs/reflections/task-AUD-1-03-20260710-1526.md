- Task: AUD-1-03 验证 hook CLI 版本
- Encountered Problem: hook CLI 虽有 --version 分支，但此前发布流程没有执行自检。
- Thought Process: 安装器引用的 hook CLI 必须能独立启动，版本输出是低成本完整性检查。
- Options Considered: 只检查文件存在；执行 --version；通过 host 间接验证。
- Chosen Solution: pack-all.ps1 在 artifact 构建后直接执行 taskbar_widget_hook.exe --version，并拒绝空输出。
- Rationale: 能同时发现缺失、不可执行和错误 artifact。
- Evidence: release hook CLI 输出 0.1.0，脚本退出码为 0。
- Status: completed
