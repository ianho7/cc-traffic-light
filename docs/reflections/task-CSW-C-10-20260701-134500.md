- Task: CSW-C-10 accept `received_at` fallback ordering when no explicit `event_order` is present
- Encountered Problem: 真实样本里仍未出现独立 `event_order`，需要决定是否把这视为阻塞。
- Thought Process: 对照计划里的已知限制和当前实现，确认本轮目标是验证状态写入闭环，而不是先扩 schema。
- Options Considered: 因缺少独立 `event_order` 阻塞本轮；修改实现强行新增字段；记录并接受现有 `received_at` 兜底。
- Chosen Solution: 继续接受 `received_at` 作为排序兜底，并在 checklist/handoff 中显式记账。
- Rationale: 这符合当前 P0 范围，也避免为补证而扩大实现面。
