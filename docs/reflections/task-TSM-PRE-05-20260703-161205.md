- Task: TSM-PRE-05 确认 workspace 改造前提
- Encountered Problem: 仓库当前不是 Cargo workspace，而 shared-core 和 Tauri Rust crate 都需要稳定落点。
- Thought Process: 我先确认根目录没有 Cargo workspace，再决定 workspace 需要先只纳入 `taskbar-widget`，避免一次性引入多个新成员。
- Options Considered: 继续保持单 crate；等 shared-core 创建后再补 workspace；先建立最小根级 workspace。
- Chosen Solution: 先建立只包含 `taskbar-widget` 的根级 workspace。
- Rationale: 这是最小扰动路径，能给后续 crates 预留位置，又不需要立刻处理多成员耦合。
