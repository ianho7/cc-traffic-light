- Task: CSW-B-04 preserve dump script for future sampling
- Encountered Problem: P0 虽然要切换默认项目 hooks，但真实 Codex 失败时仍可能需要回到 dump 路径继续采样。
- Thought Process: 只改 `.codex/hooks.json`，不改 `taskbar-widget/scripts/codex-lifecycle-hook-dump.ps1`。
- Options Considered: 删除 dump 脚本；重命名 dump 脚本；保留脚本但不再作为默认 hooks 命令。
- Chosen Solution: 保留 dump 脚本原样，仅把项目 hooks 默认命令切到真实状态写入 CLI。
- Rationale: 这让 P0 能继续推进，同时保留最便宜的回退和诊断路径。
