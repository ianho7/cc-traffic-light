- Task: GHI-VAL-01 run cargo check
- Encountered Problem: 安装脚本虽然是 PowerShell，但它最终指向的 Rust hook binary 不能在这轮里失去可构建性。
- Thought Process: 先跑最快的编译检查，确认仓库当前状态没有被脚本工作带偏。
- Options Considered: 只测脚本；直接 `cargo build`；先 `cargo check` 再 `cargo build`。
- Chosen Solution: 先运行 `cargo check`。
- Rationale: 这样能用最低成本确认 Rust 侧基线仍然健康。
