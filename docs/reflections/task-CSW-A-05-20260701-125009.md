- Task: CSW-A-05 record pre-switch baseline observation
- Encountered Problem: 如果不记录切换前基线，后续看到 `working`/`waiting` 时很难区分是新事件写入还是旧状态残留。
- Thought Process: 把切换前 `list` 的空任务、`global_summary = idle` 作为明确对照，再进行真实状态写入试验。
- Options Considered: 不记录基线；只记录 state.json 是否存在；记录 `list` 输出摘要。
- Chosen Solution: 以 `list` 输出为空任务、`idle` summary 作为 Phase 1 基线。
- Rationale: 之后无论是真实 Codex 事件还是手工 payload，都能与这个基线比较，减少对旧状态残留的误判。
