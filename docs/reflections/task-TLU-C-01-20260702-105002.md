- Task: TLU-C-01 用 debug set 验证 `idle` 的颜色、形态和可读性
- Encountered Problem: 初次状态矩阵验证时，共享状态被历史任务污染，导致 `idle` 不代表单任务场景。
- Thought Process: 先清理旧记录并隔离共享状态，再重新观察 `idle` 的基线展示。
- Options Considered: 在脏状态下继续解读结果；先隔离状态后重测。
- Chosen Solution: 使用隔离共享状态重跑 `idle` 基线。
- Rationale: 只有隔离后的 `global_summary` 才能证明 `idle` 映射本身是否正确。
