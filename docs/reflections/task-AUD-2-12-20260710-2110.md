- Task: AUD-2-12 hook CLI 缺失与状态诊断
- Encountered Problem: install-codex-hooks.ps1 原先只校验路径形状，可能把不存在的 exe 写入 hooks 配置。
- Thought Process: 增加 apply-time file existence check，dry-run 输出 hook_executable_exists；用临时 stable path copy 验证成功和缺失两条路径。
- Options Considered: 只在 installer 层检查；脚本永远允许写入；脚本 apply 阶段拒绝缺失 exe。
- Chosen Solution: apply 阶段拒绝缺失 hook executable，dry-run/apply 输出存在性。
- Rationale: 避免安装完成后静默写入不可执行的 hook path。
- Evidence: dry_exists=true、apply_exists=true；missing_error 明确指出 HookExecutablePath 不存在；fixture 和 temp exe 已清理。state 不可写仍未覆盖。
- Status: partial
