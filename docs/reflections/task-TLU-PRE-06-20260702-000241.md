- Task: TLU-PRE-06 确认本轮不修改 taskbar.rs 和 attach/render 路径
- Encountered Problem: GDI 可见性问题很容易被误诊成宿主窗口问题。
- Thought Process: 把“不改 `taskbar.rs`、不改窗口尺寸和 layered 路径”作为执行前约束，逼迫实现只在绘制层收敛。
- Options Considered: 如有需要顺手改 host 路径；先锁死宿主边界。
- Chosen Solution: 锁死宿主边界，只在 `main.rs` 做改动。
- Rationale: 这符合仓库规则，也保住了现有 Win11 稳定路径。
