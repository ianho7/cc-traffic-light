# P0-04 Reflection

- Task: 规范化无效素材配置。
- Encountered Problem: 空槽位、重复 ID 和未知引用不应传递到渲染层。
- Thought Process: 在共享配置 normalize 阶段过滤无效组，随后清除不存在的选择。
- Options Considered: host 逐处容错；保留所有无效条目；共享层规范化。
- Chosen Solution: 共享层保留第一份有效唯一 ID，并将未知 Agent 绑定设为默认。
- Rationale: 所有消费者收到一致的安全配置，host 仍可处理运行时文件丢失。
