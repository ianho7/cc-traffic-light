- Task: CSW-A-04 verify list mode reads state safely
- Encountered Problem: P0 后续需要频繁依赖 `list` 作为最小 verifier，如果 `list` 本身不稳定，后面所有观察都会变弱。
- Thought Process: 在切换 hooks 前先跑 `list`，确认它能在无任务基线下读取并展示状态摘要。
- Options Considered: 先手工写入状态再看 `list`；先看空基线输出；直接读 state.json 文件。
- Chosen Solution: 先运行 `taskbar_widget_hook.exe list` 观察空任务时的默认输出。
- Rationale: 这给了 P0 一个干净基线，也证明 CLI 在当前环境下至少能安全读取默认状态路径。
