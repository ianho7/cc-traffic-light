- Task: GTV1-C-03 Implement source-priority and same-level conflict downgrade rules
- Encountered Problem: The requirements explicitly called for fixed source priority and a downgrade to untrusted on same-level contradictory fresh evidence, but no code path enforced that yet.
- Thought Process: I encoded source-priority ranking, same-level timestamp comparison, and explicit conflict detection in detector aggregation before more sources are added.
- Options Considered: Delay conflict logic until multiple real sources exist; guess one winner silently; implement the conflict downgrade rule now against the detector abstraction.
- Chosen Solution: Implement the conflict downgrade rule now in the detector abstraction.
- Rationale: This is a contract decision, not a later polish step, and later source additions should inherit the same semantics automatically.
