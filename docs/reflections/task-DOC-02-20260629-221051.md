- Task: DOC-02 记录依赖的 Win32 API 与 `windows` crate feature
- Encountered Problem: 这类 PoC 的可维护性很依赖“知道到底用了哪些 API”。
- Thought Process: 把 crate feature 和核心 API 名单一起收敛进 README。
- Options Considered: 只写 Cargo；只写 API；两者都写。
- Chosen Solution: README 同时列出了 `windows` crate feature 和核心 API。
- Rationale: 这对后续裁剪和扩展都最有帮助。
