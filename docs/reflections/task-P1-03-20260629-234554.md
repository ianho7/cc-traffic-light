# Task Reflection

- Task: `P1-03` 拆分“宿主父窗口”和“位置锚点”的日志与诊断字段
- Encountered Problem: 旧日志用 `candidate_parent` 表达宿主，容易把“挂在哪”和“拿谁算坐标”混为一谈。
- Thought Process: 先改运行时日志，再在诊断 JSON 里增加 `host_parent`，同时保留 `candidate_parent` 兼容旧脚本。
- Options Considered: 直接改名并打破旧脚本；增加新字段并保持旧字段兼容。
- Chosen Solution: 使用 `host_parent` / `position_anchor` 作为新语义，同时保留 `candidate_parent` 别名。
- Rationale: 先把事实表述纠正，再把脚本迁移延后到后续任务。
