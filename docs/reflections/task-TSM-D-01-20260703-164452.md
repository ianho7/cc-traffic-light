- Task: TSM-D-01 新建 `taskbar-settings-tauri/` 工程并固定包管理器为 PNPM
- Encountered Problem: 当前仓库完全没有 Node/Tauri 前端工程结构，而迁移又明确要求独立的 Tauri settings app。
- Thought Process: 我采用手工 scaffold，而不是依赖外部初始化器，这样能直接对齐现有 workspace 和 Windows-only 宿主边界。
- Options Considered: 等真实 IPC 完成后再建工程；使用交互式脚手架；手工创建 `taskbar-settings-tauri/` 并固定 `PNPM` 工作区。
- Chosen Solution: 新建 `taskbar-settings-tauri/`，提供 `package.json`、TypeScript/Vite 配置和 `src-tauri/` Rust crate。
- Rationale: 手工 scaffold 更可控，也避免初始化器在当前仓库里引入不必要的模板噪音。
