- Task: AUD-VAL-06 release hook CLI build
- Encountered Problem: 无。
- Thought Process: 独立构建 hook bin，确认它不是依赖 host 默认 target 的隐式副产物。
- Options Considered: 依赖 host 构建；单独构建 hook bin；使用旧 target artifact。
- Chosen Solution: cargo build -p taskbar-widget --bin taskbar_widget_hook --release --offline。
- Rationale: 发布脚本和 installer 需要明确、同版本的 hook CLI。
- Evidence: target/release/taskbar_widget_hook.exe 生成，version self-check 输出 0.1.0。
- Status: completed
