- Task: GHI-B-01 initialize install-codex-hooks.ps1
- Encountered Problem: P1 还没有用户级 hooks 安装器，只有项目级配置和 notify 参考脚本。
- Thought Process: 先把脚本骨架落地，再把安装契约和 merge 逻辑收进一个明确入口。
- Options Considered: 继续只写计划文档；复用 notify 脚本硬改；新建独立 lifecycle 安装脚本。
- Chosen Solution: 新增独立的 `taskbar-widget/scripts/install-codex-hooks.ps1`。
- Rationale: 这样不会污染 notify 路径，也便于后续独立验证 dry-run/apply/restore。
