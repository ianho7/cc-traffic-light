- Task: AUD-1-02 显式检查三个 release artifact
- Encountered Problem: 原 Show-Artifact 在文件缺失时只警告并继续，不能作为发布 gate。
- Thought Process: 缺少任意一个 exe 都应阻止后续安装包编译，避免产出不完整安装器。
- Options Considered: 保留 warning；新增必需 artifact 检查；只在 installer 编译时检查。
- Chosen Solution: 将 pack-all.ps1 的 Show-Artifact 缺失行为改为 throw，并输出 host、settings、hook CLI 三个 artifact。
- Rationale: 构建脚本本身是最早、最便宜的独立 verifier。
- Evidence: 三个路径均输出文件大小和时间，脚本退出码为 0。
- Status: completed
