- Task: TLU-A-01 定义 idle/working/waiting/done/error 的视觉契约
- Encountered Problem: 当前 UI 只有背景色和完整状态词，没有“主形态”和“对比层次”。
- Thought Process: 先从现有状态色出发，但把更亮的状态灯和更短的标签作为主识别信号。
- Options Considered: 纯文本；纯圆点；圆点加紧凑标签。
- Chosen Solution: 采用状态底色 + 高对比状态灯 + 紧凑标签的契约。
- Rationale: 这样既保留现有可见性路径，又把第一眼识别从文本转移到形状和亮度。
