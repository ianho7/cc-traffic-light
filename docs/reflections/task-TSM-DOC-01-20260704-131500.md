- Task: TSM-DOC-01 新增或更新迁移架构说明，包含进程边界图和通信方向说明
- Encountered Problem: 之前虽然有架构文字，但缺少一眼能看懂的进程边界和通信方向表示，交接效率不够高。
- Thought Process: 我优先补一张能对应当前真实实现的边界图，而不是再写一段更长的纯文字。
- Options Considered: 只保留文字；单独做图片；在现有 markdown 文档里加入 Mermaid 边界图和通信说明。
- Chosen Solution: 在 `docs/plan/tauri-settings-architecture-baseline.md` 中加入 `taskbar-widget.exe -> taskbar-settings-tauri.exe -> shared-core -> config.json` 的 Mermaid 图和配套说明。
- Rationale: Markdown 内联图最容易随代码演进一起维护，也足够表达当前进程模型。
