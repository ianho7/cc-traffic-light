- Task: CCH-D-01 将 Claude 本地实验 hooks 从 sample 模式切到真实状态写入
- Encountered Problem: shape-only 证据已经足够，如果不切到真实状态写入，P2 就无法推进到共享状态验证阶段。
- Thought Process: 在确认 `UserPromptSubmit`、`PreToolUse`、`PostToolUse`、`Notification`、`PostToolUseFailure`、`Stop` 都稳定提供 `session_id` 和 `hook_event_name` 后，把 `.claude/settings.local.json` 的 command 从 sample 脚本切回 `taskbar_widget_hook.exe claude <HookName>`。
- Options Considered: 继续停留在 sample；切到真实状态写入。
- Chosen Solution: 切到真实状态写入。
- Rationale: 当前主要风险已从“字段未知”转移为“状态写入是否正确”，应该进入下一验证阶段。
