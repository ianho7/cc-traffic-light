- Task: HC-VAL-01 through HC-VAL-15, HC-LOOP-VAL-01, HC-LOOP-VAL-02
- Encountered Problem: 普通沙箱下 `cargo build` 写 `target\debug\.cargo-lock` 被拒绝；PowerShell 写入带 BOM 的 JSON 会让状态读取误判为损坏。
- Thought Process: 构建失败属于环境权限问题，按策略请求提升权限验证；BOM 问题属于 Windows 人工验证兼容性，应在读取层处理而不是要求用户规避。
- Options Considered: 跳过 build；要求用户手工构建；请求提升权限构建；对 BOM 文件当作损坏恢复；读取时剥离 BOM。
- Chosen Solution: 使用已审批的 `cargo build` 完成构建；状态读取支持 UTF-8 BOM；用临时目录完成 working/waiting/error/Stop/乱序/缺 session/stale/corrupt/debug/并发验证。
- Rationale: 验证覆盖了当前可自动检查的核心风险；GUI 肉眼重绘和 Phase 5 图片能力需要用户配合，保留未完成项。
