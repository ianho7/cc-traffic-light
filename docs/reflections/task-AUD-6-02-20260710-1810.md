- Task: AUD-6-02 hook status DTO 与 React 类型
- Encountered Problem: Claude 进程降级状态此前没有跨 shared-core、Rust bridge 和 React 的统一表达。
- Thought Process: 增加 ProcessOnly 到共享 HookStatus，并同步 Rust as_str、TypeScript union 和 UI 分支。
- Options Considered: 继续复用 not_installed；只在 UI 本地判断；扩展共享 DTO。
- Chosen Solution: 扩展共享 HookStatus 为 process_only。
- Rationale: 保持 IPC 合约单一，避免 UI 猜测 backend 状态。
- Evidence: cargo check、cargo test --workspace、npx tsc -p tsconfig.app.json --noEmit 全部通过。
- Status: completed
