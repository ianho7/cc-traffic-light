- Task: VAL-01 `cargo check` 通过，且没有因为过度抽象引入不必要模块
- Encountered Problem: 首次编译暴露了 `UpdateWindow` 导入、`CreateWindowExW` 实例参数和 `DrawTextW` 签名三处偏差。
- Thought Process: 把编译器错误当成 API 校准器，逐项修正，避免在未通过类型检查前继续扩写功能。
- Options Considered: 暂时忽略编译问题继续写更多代码；回退到更简单占位；修正签名后重新验证。
- Chosen Solution: 修正 `windows 0.58` 的真实调用方式，并在无警告状态下重新运行 `cargo check`。
- Rationale: 对 Rust + Win32 这类底层 PoC，类型检查通过本身就是重要阶段证据。
