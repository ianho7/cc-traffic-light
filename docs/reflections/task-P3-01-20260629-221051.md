- Task: P3-01 在保留普通窗口基线的前提下，为目标窗口引入最小状态结构，至少保存自身句柄、任务栏句柄、父句柄和目标矩形
- Encountered Problem: 一旦进入嵌入和定位阶段，单靠局部变量已经不足以表达当前窗口的真实状态。
- Thought Process: 只保留后续阶段绝对要用到的字段，避免把状态对象做成平台抽象。
- Options Considered: 不建状态；建复杂 host/context 对象；保留最小 `AppState`。
- Chosen Solution: `AppState` 只保存 `hwnd`、`taskbar_hwnd`、`parent_hwnd`、`module_rect`。
- Rationale: 这与 checklist 和计划文档保持一致，也为 Phase 4 和 Phase 5 提供了最少但够用的状态。
