- Task: AUD-6-05 Tauri settings runtime lifecycle and post-deploy refresh
- Encountered Problem: release lifecycle verifier 的 spawn/reuse 阶段通过，但向 Tauri settings 窗口发送关闭消息后，settings 进程在 20 秒内没有退出；第一次运行还暴露了 verifier 可能按同标题误选 Win32 fallback 的问题。
- Thought Process: 先修正 verifier 为按 settings 子进程 PID 查找窗口，再只重跑一次；第二次仍在同一 close 阶段超时，因此不能把代码检查或 spawn/reuse 结果扩大为完整运行时通过。
- Options Considered: 重复增加 timeout；强制杀进程后伪造 graceful close；记录 report 并等待真实 Tauri/Windows 行为进一步定位。
- Chosen Solution: 修正 verifier 的窗口定位并保留失败 report；不把 AUD-6-05 标记完成，停止重复运行。
- Rationale: `WM_CLOSE` 后进程未退出是当前真实桌面运行时证据，不应通过强杀或延长等待掩盖。
- Evidence: `taskbar-widget/target/validate-tauri-settings-lifecycle/report.json`：`open_spawned_once=true`、`reopen_reused_same_process=true`、`close_exited_process=false`；runtime log 记录 host/settings spawn/reuse；两次 release verifier 均在 close wait 超时，finally 已清理进程。
- Status: blocked
