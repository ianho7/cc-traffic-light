- Task: TLU-D-05 当真实 hook 与人工 `set` 不一致时定位问题归属
- Encountered Problem: 真实失败请求没有落到 `error`，批准路径也没有稳定退出 `waiting`，与人工状态矩阵结果不一致。
- Thought Process: 比较三类真实 prompt 的状态序列，判断问题是在绘制层还是事件映射层。
- Options Considered: 继续调 UI 颜色和标签；把问题定位为真实 hook 状态映射异常。
- Chosen Solution: 记录为事件映射问题，而不是绘制问题。
- Rationale: 成功路径已经能 `run -> done`，说明绘制层基本正常；失败和批准路径异常更符合 hook 规则或状态归并错误。
