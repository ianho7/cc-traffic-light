- Task: AUD-4-03 多 session 并发写入
- Encountered Problem: 并发测试的第一版 harness 使用 JSON format string，导致测试命令自身报错，不能作为证据。
- Thought Process: 按闭环规则修正 harness 一次，改为字符串拼接，要求每个 job 返回成功且 state task 数量等于 6。
- Options Considered: 顺序写入；使用 Start-Job 并行启动 hook CLI；直接操作 state JSON。
- Chosen Solution: 使用 6 个 Start-Job 并行调用 release hook CLI，隔离 TASKBAR_WIDGET_STATE_HOME。
- Rationale: 覆盖真实进程级并发和 state mutex，不直接绕过写入逻辑。
- Evidence: 6 个 Codex jobs 写入 state tasks=6；随后 2 个 Codex + 2 个 Claude 并行写入，state tasks=4、codex active_task_count=2、claude active_task_count=2；临时目录已清理。
- Status: completed
