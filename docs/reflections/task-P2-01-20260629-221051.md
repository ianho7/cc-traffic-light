- Task: P2-01 在 `taskbar.rs` 中实现 `FindWindowW("Shell_TrayWnd")` 并打印句柄
- Encountered Problem: 在沙箱会话里直接查 `Shell_TrayWnd` 得到的是空结果，容易误判成实现失败。
- Thought Process: 先确认 API 签名，再区分“代码失败”和“桌面会话隔离”两类问题。
- Options Considered: 认定当前机器没有 `Shell_TrayWnd`；继续在沙箱里盲调；切到可访问交互桌面的提权会话验证。
- Chosen Solution: 在 `taskbar.rs` 中实现真实探测逻辑，并用提权运行拿到 `Shell_TrayWnd=0x10102` 的证据。
- Rationale: 这证明问题是会话可见性，不是 Win32 探测逻辑本身错误。
