- Task: RLS-3-01 至 RLS-3-03 Settings lifecycle
- Encountered Problem: 第二次打开将刚启动的 Tauri 进程误判为无可见窗口并终止。
- Thought Process: runtime log 显示仅 199ms 的启动窗口；脚本已证明该 PID 有 top-level HWND。
- Options Considered: 延长 timeout、重试启动、按 PID 复用 HWND。
- Chosen Solution: 不再要求 HWND 先可见；按 PID 找到顶层窗口后以 `ShowWindow(SW_RESTORE)` 聚焦。
- Rationale: 消除 startup race，同时维持关闭即退出的 verifier 语义。
- Evidence: `taskbar-widget/target/validate-tauri-settings-lifecycle/report.json`（debug/release 均全部 true，最新为 release）；workspace tests 与独立 release builds。
- Status: completed
