- Task: TSM-I-05 审计仓库，确认没有遗留看起来还在用实际上已废弃的双轨 settings 代码
- Encountered Problem: 迁移后最容易留下的是“默认入口已换，但源码和文档仍在暗示旧入口还活着”的伪双轨状态。
- Thought Process: 我分别审计了活跃源码引用、构建依赖、事件循环、tray 打开路径和当前架构文档，确认当前活跃链路已经不再经过 Slint。
- Options Considered: 1. 只审代码。2. 只审 docs。3. 代码、构建、运行时、文档一起审。
- Chosen Solution: 同步清理源码与构建引用，并更新 FILETREE、架构 baseline、migration checklist 和 handoff。
- Rationale: 双轨问题是认知层和实现层共同造成的，只清一边会留下新的误导。
