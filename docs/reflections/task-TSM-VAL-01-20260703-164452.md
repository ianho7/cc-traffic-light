- Task: TSM-VAL-01 cargo check 通过
- Encountered Problem: 新增了 shared-core、Tauri src-tauri crate 和前端 scaffold 后，最直接的风险是 workspace 被打断。
- Thought Process: 我先分模块验证，再用 `cargo check --workspace --offline` 做全仓验证，确保新成员纳入 workspace 后主工程仍可编译。
- Options Considered: 只检查旧 crate；只检查新 Tauri crate；做 workspace 级 `cargo check`。
- Chosen Solution: 以 `cargo check --workspace --offline` 作为当前 Rust 侧主验证证据。
- Rationale: 这能同时证明旧宿主、新 shared-core 和新 Tauri Rust crate 没有在编译层面互相打断。
