- Task: GTV1-VAL-04 通用设置项可保存并重新加载
- Encountered Problem: `General` 页开关此前只完成了代码实现和局部编译验证，没有真实证明保存后的状态能在重新打开窗口后保持一致。
- Thought Process: 该项需要验证“修改 -> 保存 -> 关闭 -> 重新打开”的完整往返链路。
- Options Considered: 1. 只确认点击不报错。2. 只检查 `config.json` 写盘。3. 用真实 UI 验证开关保存和重新加载。
- Chosen Solution: 使用人工联调确认通用设置项可以保存，并在重新打开 settings 后正确重新加载。
- Rationale: 用户看到的结果才是最终需求，单纯写盘并不等于真实可用。
