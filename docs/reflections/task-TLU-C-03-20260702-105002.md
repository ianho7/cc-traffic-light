- Task: TLU-C-03 用 debug set 验证 `waiting` 的颜色、形态和可读性
- Encountered Problem: `waiting` 在全局 summary 中优先级较高，之前容易与历史记录混在一起。
- Thought Process: 用单任务 `waiting` 场景确认黄色状态灯和 `wait 1` 标签是否一眼可辨。
- Options Considered: 依赖旧观察；单独重测 `waiting`。
- Chosen Solution: 单独执行 `set codex_123 waiting` 并记录结果。
- Rationale: 该状态在真实 hook 场景中会频繁出现，必须先确认基础映射稳定。
