- Task: SSM-F-02 保留旧 settings_window.rs 作为短期 fallback
- Encountered Problem: Slint runtime 现在刚接上独立线程 event loop，仍需要保留一个可靠回退路径以防平台整合细节出问题。
- Thought Process: 不删除旧窗口，只把它的角色降级为 fallback，并继续同步 snapshot。
- Options Considered: 立即删除 Win32 settings；保留并继续作为默认；保留但降级为 fallback。
- Chosen Solution: 保留 `settings_window.rs`，并在 Slint show 失败时回退显示。
- Rationale: 这符合 checklist 对短期 fallback 的要求，也让后续人工联调更安全。
