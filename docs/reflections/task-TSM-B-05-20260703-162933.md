- Task: TSM-B-05 为 shared-core 补最小测试
- Encountered Problem: 迁移配置和状态模型到新 crate 后，如果没有测试，最容易在默认值、兼容性或投影层上悄悄回归。
- Thought Process: 我优先覆盖这轮真正新增或迁出的风险点：配置 round-trip、缺失字段默认值、兼容反序列化，以及状态投影字段保真。
- Options Considered: 先不补测试；只保留原有 config 测试；在 shared-core 内补 config 和 snapshot projection 两类最小测试。
- Chosen Solution: 保留并迁移 `AppConfig` 相关测试，同时新增 `snapshot_projection_preserves_source_fields`。
- Rationale: 这足以证明 shared-core 目前承接的边界没有在最基础的序列化和投影层面破掉。
