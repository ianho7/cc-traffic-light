- Task: CSW-B-01 capture dump-hook command shape before switching
- Encountered Problem: P0 要从 dump 切到真实写入，但后续仍可能需要重新采样，如果完全失去原始 command shape，会增加回退成本。
- Thought Process: 在编辑 `.codex/hooks.json` 之前先读取当前 JSON，确认每个事件目前都指向 dump 脚本。
- Options Considered: 直接覆盖不留证据；读当前 hooks.json；另存一个备份文件。
- Chosen Solution: 读取并记录当前 `.codex/hooks.json` 的 dump-command 结构，同时保留脚本文件不删除。
- Rationale: 这足以证明切换前状态，也保留了后续按同一事件集回到采样模式的能力，而不需要额外生成备份文件。
