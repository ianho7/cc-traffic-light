- Task: SSM-PRE-02 阅读 main.rs、settings_window.rs、tray_icon.rs 并确认入口
- Encountered Problem: 迁移 settings host 前，必须先知道当前 settings/tray/runtime 的真实入口和耦合点。
- Thought Process: 从 `main.rs` 入口、tray actions、`settings_window` 生命周期和 `tray_icon` 菜单/tooltip 输出链路逐层确认。
- Options Considered: 仅凭之前记忆推进；重新阅读当前源码并记录入口职责。
- Chosen Solution: 重新阅读当前源码，确认 `main.rs` 持有主循环与 settings 打开入口，`settings_window.rs` 持有现有 settings 壳子，`tray_icon.rs` 持有菜单和 tooltip。
- Rationale: 只有先锁定入口，后续才能把 Slint host 插在正确位置，而不是改错宿主层。
