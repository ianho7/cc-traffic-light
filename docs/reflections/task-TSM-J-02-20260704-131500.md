- Task: TSM-J-02 更新运行和构建文档，加入 PNPM、Tauri、workspace 相关命令
- Encountered Problem: 迁移后的关键构建约束不是“怎么编译”，而是“哪些命令不能拿来验收宿主”，否则会再次产出带 `TaskDialogIndirect` 的错误宿主。
- Thought Process: 我把当前已验证的命令集合和 workspace feature 污染结论绑定起来写，避免团队只记住命令、不理解为什么必须分包构建。
- Options Considered: 只在脚本里改命令；只在 handoff 里提一句；同时在架构文档和 handoff 中都固定推荐命令与禁用路径。
- Chosen Solution: 在 `docs/plan/tauri-settings-architecture-baseline.md` 和 `docs/handoff/2026-07-04-0130.md` 中同时加入 `pnpm build`、单包 `cargo build -p ...` 以及禁止用 `cargo build --workspace` 做宿主验收的说明。
- Rationale: 构建约束本身就是迁移的一部分；只改脚本不改文档，后续很容易被再次踩回旧问题。
