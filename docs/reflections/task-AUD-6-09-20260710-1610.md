- Task: AUD-6-09 重新检测入口
- Encountered Problem: Tauri MonitoringPage 只有部署按钮，没有用户可触发的立即重新检测入口。
- Thought Process: 刷新必须经过 host 的 request_refresh，再读取新的 snapshot 和 hook status；失败不能静默吞掉。
- Options Considered: 依赖 5 秒轮询；只刷新前端缓存；调用 request_refresh 后立即重新读取两份状态。
- Chosen Solution: 增加“立即刷新检测”按钮，显示刷新中/成功/失败状态，并在成功后更新 snapshot 与 hook status。
- Rationale: 用户可以在 trust、部署或修复配置后立即验证，而不需要等待轮询或重启设置页。
- Evidence: `MonitoringPage.tsx`、`lib/tauri.ts`；`npx tsc -p tsconfig.app.json --noEmit` passed。
- Status: completed
