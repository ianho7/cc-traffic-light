- Task: TLU-VAL-08 验证文本长度受控，没有回退到完整状态词主导的旧 UI
- Encountered Problem: 保留文本后，UI 容易不自觉回到“文字为主”的旧样式。
- Thought Process: 通过 `run / wait / done / err` 这些紧凑标签验证文本是否被限制在最小必要范围内。
- Options Considered: 保留完整状态词；用紧凑标签加可选计数。
- Chosen Solution: 采用紧凑标签，并把观察结果记录为通过。
- Rationale: 当前实际输出已经证明文本长度受控，没有重新挤压图形层。
