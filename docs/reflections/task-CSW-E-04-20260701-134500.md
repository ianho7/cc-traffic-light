- Task: CSW-E-04 document how to roll back to dump mode
- Encountered Problem: 项目默认 hooks 已经切到真实状态写入，如果不把回退路径写清，后续重新采样 payload 还会重复探索。
- Thought Process: 直接记录“把 `.codex/hooks.json` 指回 dump 脚本”这条最小回退动作，并强调 dump 的用途只是再采样。
- Options Considered: 不写回退说明；只在 README 提一句；在 handoff 中明确写回退步骤。
- Chosen Solution: 在 handoff 中加入回退说明，引用现存 dump 脚本。
- Rationale: 这是最短的运维信息闭环，不需要再改实现也能指导下一轮排障。
