- Task: CCH-B-05 触发真实 PostToolUseFailure shape-only sample
- Encountered Problem: 如果没有真实失败事件样本，错误态映射会继续停留在猜测层。
- Thought Process: 触发一个必然失败的读文件调用，再读取 `.claude/hook-logs/PostToolUseFailure.jsonl`。
- Options Considered: 等以后再补；现在就用一个稳定失败路径补样本。
- Chosen Solution: 现在就补。
- Rationale: 当前失败样本已经足够说明 Claude 在失败态会提供 `error`、`is_interrupt`、`tool_name`、`tool_input` 和 `session_id`。
