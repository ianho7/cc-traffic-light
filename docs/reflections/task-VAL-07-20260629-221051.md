- Task: VAL-07 至少两轮回归结果一致；若不一致，视为未通过 MVP 验证
- Encountered Problem: 需要一个简单且严格的“一致性”定义。
- Thought Process: 用 `Attached`、`Moved`、`Destroyed` 三个布尔信号定义一次闭环是否成立。
- Options Considered: 只比对日志文本；只比对退出码；用关键运行信号判断一致性。
- Chosen Solution: 两轮结果都满足 `ExitCode=0`、`Attached=true`、`Moved=true`、`Destroyed=true`。
- Rationale: 这已经足以表明当前 MVP 不是偶然成功。
