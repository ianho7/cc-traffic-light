- Task: DOC-04 记录最小调试输出字段：任务栏句柄、父句柄、错误码、矩形、最终坐标
- Encountered Problem: 如果不定义“最小调试字段”，日志会越长越散。
- Thought Process: 只保留对当前阶段有用的句柄、矩形、样式和位置输出。
- Options Considered: 无约束日志；上大日志系统；定义最小关键字段。
- Chosen Solution: 通过 `log_probe`、`log_attach`、`log_layout` 固定了关键输出集合。
- Rationale: 这兼顾了诊断性和简洁性。
