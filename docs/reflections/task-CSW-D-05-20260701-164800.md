- Task: CSW-D-05 record the updated manual observation conclusion after the real fast-sampling attempts
- Encountered Problem: 13:45 的结论只到“真实状态源可显示 summary，但 live redraw partial”；16:40 之后又出现了两轮更短时序的实证，需要更新结论粒度。
- Thought Process: 把 fresh 启动时的 `WAITING 2/3` 正证据、真实 `active_task_count = 3/4` 正证据、以及 after 为空白的负证据放到同一个结论里。
- Options Considered: 维持旧结论不动；直接把 empty after 当 redraw fail；收紧为“widget after 阶段退出或不可见”的运行期 blocker。
- Chosen Solution: 更新结论为“hooks 和状态链路已通过，真实源显示已通过，但同实例 after 阶段反复退出或不可见，live redraw 仍 partial”。
- Rationale: 这比单纯写成 partial 更有操作性，也能防止下一轮继续在 hook 侧空转。
