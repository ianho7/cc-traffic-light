- Task: CCH-VAL-09 记录排序仍依赖 received_at
- Encountered Problem: 真实 Claude sample 没给独立事件顺序字段，如果文档不明确，后续容易误以为已有原生排序依据。
- Thought Process: 根据 `event_order_candidates=[]` 的真实结果，回写文档接受 `received_at` 兜底。
- Options Considered: 隐含接受；显式记录。
- Chosen Solution: 显式记录。
- Rationale: 这能避免后续实现错误地假设 Claude 已提供排序字段。
