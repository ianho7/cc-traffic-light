- Task: AUD-6-05 Codex redeploy status refresh
- Encountered Problem: 设置页部署和状态刷新逻辑存在，但当前环境没有完整 frontend build 和 Tauri settings runtime evidence。
- Thought Process: 检查 handleDeployHooks：先调用 installCodexHooks，再调用 getHookStatus 更新 UI；同时增强 backend failure output。
- Options Considered: 只改按钮文案；增加后端诊断；等待真实 Tauri runtime 后再处理。
- Chosen Solution: 先完成代码级刷新链路和失败输出，标记 runtime verification 为 partial。
- Rationale: 先消除静默失败，再等待 Tauri/安装器环境可用时做运行时验收。
- Evidence: cargo check -p taskbar-widget --offline；npx tsc -p tsconfig.app.json --noEmit；MonitoringPage.tsx 和 settings_bridge.rs。
- Status: partial
