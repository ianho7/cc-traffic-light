- Task: TSM-I-03 将旧 Slint settings 相关 UI 资源和说明文档归档到 archive
- Encountered Problem: 直接删除旧 Slint 文件会丢失迁移参考；继续放在活跃源码路径下又会误导后续维护者。
- Thought Process: 归档需要同时满足两点：一是保留实现和视觉参考，二是让当前活跃源码审计时不再把 Slint 识别成在用代码。
- Options Considered: 1. 直接删除。2. 留在原路径但取消编译。3. 移入 `archive/` 并补归档说明。
- Chosen Solution: 将 `settings_slint.rs` 与 `settings.slint` 移入 `archive/slint-settings/`，并新增 README 说明归档范围和现状。
- Rationale: 这符合 checklist 对 archive 的要求，也让后续仓库审计更清晰。
