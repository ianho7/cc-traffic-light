- Task: GHI-D-06 apply the installer to the real user-scoped hooks.json after approval
- Encountered Problem: 真实用户级写入是 P1 第一条不可逆风险更高的路径，必须先确认稳定 exe 路径，再避免把不存在的 hooks 文件误当成合并场景。
- Thought Process: 先把 `taskbar_widget_hook.exe` 复制到稳定安装目录，再执行真实 `apply`，最后用第二次 dry-run 验证写入后是否已经达到稳定状态。
- Options Considered: 继续停在 dry-run；直接把 hooks 指向 workspace build 路径；复制到稳定路径后再 apply 并复核。
- Chosen Solution: 复制 binary 到 `C:\\Users\\admin\\AppData\\Local\\CcTrafficLight\\bin\\taskbar_widget_hook.exe`，然后对 `C:\\Users\\admin\\.codex\\hooks.json` 执行真实 `apply`。
- Rationale: 这样既满足了稳定 command hash 的契约，也把真实安装验证收敛成“首次安装 -> 二次 dry-run unchanged”的闭环。
