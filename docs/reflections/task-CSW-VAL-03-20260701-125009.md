- Task: CSW-VAL-03 validate list mode shows current summary
- Encountered Problem: `list` 是 P0 后续真实 Codex 验证的关键 observer，如果它不能稳定输出当前摘要，后续证据链会断。
- Thought Process: 在空基线、手工写入、清理回空三种状态下都观察 `list` 输出，确认它能反映当前 state。
- Options Considered: 只看一次 `list`；直接读 state.json；同时观察 `list` 与 state.json。
- Chosen Solution: 以 `list` 为主 verifier，并交叉查看 state.json 内容。
- Rationale: 这证明 CLI 观察口与持久化状态是一致的，足以支撑下一步真实 Codex 会话验证。
