- Task: SSM-D-04 让 tray 和 settings 共用统一 i18n 层
- Encountered Problem: 即使 Phase 3 已建立共享 i18n 模块，主入口切换前仍需要证明 tray 和 Slint settings 都真的在消费它。
- Thought Process: 从实际调用链验证 `tray_icon.rs` 和 `settings_slint.rs` 都通过 `Localizer` 输出文本，而不是保留第二套逻辑。
- Options Considered: 保留现状不补文档；把共享调用链明确写入 checklist 并作为完成依据。
- Chosen Solution: 以当前代码状态确认 D-04 完成，并在 checklist 中记录 tray/settings 共用 i18n 模块的事实。
- Rationale: 这能防止后续继续扩 UI 时又回到“双文本系统”的状态。
