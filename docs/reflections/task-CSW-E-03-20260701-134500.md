- Task: CSW-E-03 record known limitations around missing explicit `event_order` and unresolved `PostToolUse` failure mapping
- Encountered Problem: 如果只记录已通过项，后续很容易误以为 payload 细节和失败映射也已经收紧完毕。
- Thought Process: 把“仍接受 received_at 兜底”和“PostToolUse 失败映射仍待真实 payload”作为显式限制写入 checklist 与 handoff。
- Options Considered: 暂不写限制；在 README 大幅展开；只在 handoff 简单带过。
- Chosen Solution: 在 checklist 判定区和 handoff 中都保留这两个限制。
- Rationale: 这能约束下一轮不要把未解决的问题当成已经完成。
