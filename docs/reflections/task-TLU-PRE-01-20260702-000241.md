- Task: TLU-PRE-01 阅读 P3 README，确认 P3 目标是 UI 演进，不是宿主挂载路径重构
- Encountered Problem: P3 README 同时提到可见性、状态映射和真实 hook 验证，容易把 UI 问题误扩展到 taskbar host 路径。
- Thought Process: 先用计划文档重新划定范围，确认本轮的成功条件是“绘制层可读性提升且 attach 路径不回退”。
- Options Considered: 直接从 `main.rs` 开改；先读 README 锁定范围。
- Chosen Solution: 先读 README 并把“不改 `taskbar.rs`”当作本轮硬边界。
- Rationale: 这样能避免在没有证据时把 UI 改造演变成宿主层重构。
