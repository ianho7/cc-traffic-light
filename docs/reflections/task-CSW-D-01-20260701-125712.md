- Task: CSW-D-01 run the taskbar widget and keep it polling shared state
- Encountered Problem: 之前只有代码阅读和日志推断，没有运行中的 widget 证据，无法证明它真的消费共享状态。
- Thought Process: 用隔离状态目录启动 `taskbar-widget.exe`，避免污染 `%APPDATA%`，再通过 debug CLI 驱动状态变化。
- Options Considered: 只读源码；直接在默认状态目录运行；使用隔离目录运行并驱动状态变化。
- Chosen Solution: 在 `taskbar-widget/target/p0-redraw-verify-20260701-2/state` 下运行 widget。
- Rationale: 这样既能验证 polling/repaint 行为，又不影响真实 P0 后续要验证的 Codex 状态文件。
