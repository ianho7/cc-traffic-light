- Task: CCH-DOC-04 更新 examples.claude-hooks.json 的结构和事件集合
- Encountered Problem: 现有示例文件使用旧平面结构和未验证的事件名集合，会误导后续接入。
- Thought Process: 根据 Claude v2.1.50 的 `/hooks` UI 和真实 sample，把示例改成 matcher-group 结构，并将事件集合调整为 `UserPromptSubmit`、`PreToolUse`、`PostToolUse`、`Notification`、`PostToolUseFailure`。
- Options Considered: 保持旧示例；更新为当前已验证结构。
- Chosen Solution: 更新为当前已验证结构。
- Rationale: 这能显著降低后续使用者继续按错误模板接入的概率。
