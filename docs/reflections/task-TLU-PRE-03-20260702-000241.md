- Task: TLU-PRE-03 阅读 agent_state.rs，确认当前只消费 global_summary
- Encountered Problem: 如果现有 widget 已经部分依赖按 agent 数据，本轮布局决策就不能只围绕单 summary。
- Thought Process: 检查 `HookSummary`、`HookMonitorState` 和 `load_state_for_display_diagnostic`，确认当前可用显示信号。
- Options Considered: 假设只有全局 summary；读取状态模型确认。
- Chosen Solution: 读取 `agent_state.rs`，确认本轮只依赖 `global_summary` 的 `state`、`active_task_count` 和 stale 信号。
- Rationale: 这让 P3 可以维持单组件 UI，而不用提前设计多列布局。
