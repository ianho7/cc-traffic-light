- Task: SSM-PRE-03 阅读 app_config.rs 和 ui_state.rs 并确认 schema 与状态契约
- Encountered Problem: 需要判断当前哪些设置已经具备持久化落点，哪些只是运行态或内部 key。
- Thought Process: 审核 `AppConfig` 的 general/monitoring/appearance/diagnostics 结构，同时对照 `ui_state.rs` 中枚举和 snapshot 字段。
- Options Considered: 先做 Slint 再补 schema；先审计 schema 和状态契约再迁移 UI。
- Chosen Solution: 先审计持久化与状态契约，确认语言配置缺失、状态/方法标签仍缺少本地化展示层。
- Rationale: 没有 schema gate 和统一 label 映射，Slint 只会把旧问题换个界面继续存在。
