- Task: TSM-PRE-01 审计当前 settings 打开链路和 fallback 链路
- Encountered Problem: 当前仓库已经存在 Slint 与 Win32 双轨 settings，如果不先确认真实入口，很容易在 Tauri 迁移时误判谁是默认主入口。
- Thought Process: 我从 `main.rs` 的 tray action 入口往下追，确认 `Open Settings` 先尝试 `settings_slint::show`，失败才显示 `settings_window.rs` 的 Win32 fallback。
- Options Considered: 只看 `settings_window.rs`；只看 `settings_slint.rs`；沿 `main.rs` 实际调用链把打开和回退链路一起确认。
- Chosen Solution: 以 `main.rs` 为真实入口，完整确认默认 Slint + Win32 fallback 的现状。
- Rationale: 这能避免后续把 Tauri 迁移错误地建在 fallback 路径上。
