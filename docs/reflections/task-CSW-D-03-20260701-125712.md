- Task: CSW-D-03 record visible widget text and color changes
- Encountered Problem: 仅有 stdout 的 `summary changed` 还不够，必须区分“状态文件更新”与“任务栏界面真的变了”。
- Thought Process: 对同一任务栏区域抓取 `before/working/waiting/done` 四张截图，并计算模块区域像素差异。
- Options Considered: 只看日志；只看 state.json；同时看截图、像素差异和日志。
- Chosen Solution: 保存任务栏截图，并结合 `MeanDelta` 和状态标签颜色变化确认真正重绘。
- Rationale: 这比单纯日志更强，能直接证明任务栏上的文本和背景色确实发生了变化。
