- Task: AUD-6-04 监控诊断详情
- Encountered Problem: snapshot 已经携带 method、confidence、updated_at、message 和 last_error，但设置页没有展示这些证据，也没有展示配置路径。
- Thought Process: 用户排查“为什么没有闪烁”至少需要知道读取的路径、最近刷新、来源方法、可信度和错误。
- Options Considered: 只显示 hook 总状态；复用总览卡片；在 MonitoringPage 增加只读诊断区并传入 bootstrap 的 config_path。
- Chosen Solution: 增加诊断区，展示配置路径、挂载状态、最近刷新、最近错误，以及 Codex/Claude 的状态、来源、可信度、最近事件和备注。
- Rationale: 将已有后端证据直接暴露给用户，降低“看起来未安装但实际是未触发/降级”的歧义。
- Evidence: `taskbar-settings-tauri/src/pages/MonitoringPage.tsx`；`npx tsc -p tsconfig.app.json --noEmit` passed。
- Status: completed
