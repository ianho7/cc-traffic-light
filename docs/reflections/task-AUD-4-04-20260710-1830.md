- Task: AUD-4-04 hook CLI 输入编码与空输入
- Encountered Problem: decode_stdin_bytes 和 parse_json_input 原先只有实现，没有直接覆盖这些边界的单元测试。
- Thought Process: 将 plain UTF-8、UTF-8 BOM、UTF-16 LE BOM、空 hook input 和空 sample input 分成独立断言。
- Options Considered: 只依赖手工管道测试；新增 bin tests；重写 stdin 解码逻辑。
- Chosen Solution: 保持现有实现，新增 4 个 focused unit tests。
- Rationale: 用最小改动把已有支持变成可回归验证的证据。
- Evidence: cargo test -p taskbar-widget --bin taskbar_widget_hook --offline，4 passed。
- Status: completed
