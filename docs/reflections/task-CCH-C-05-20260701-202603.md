- Task: CCH-C-05 用真实 Claude sample 更新采样结论文档
- Encountered Problem: checklist 和采样文档还停留在“Claude 待采样”，与当前真实证据不一致。
- Thought Process: 把 `UserPromptSubmit`、`PreToolUse`、`PostToolUse` 的真实字段结论回写到 `hook-payload-sampling.md`，并明确哪些字段仍然缺失。
- Options Considered: 等更多事件后再统一更新；先按已获取证据及时更新。
- Chosen Solution: 先按已获取证据及时更新。
- Rationale: 这样能保证后续状态写入阶段以最新证据为基线。
