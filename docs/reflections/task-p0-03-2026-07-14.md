# P0-03 Reflection

- Task: 升级 schema 并兼容旧配置。
- Encountered Problem: 已部署的 schema 4 配置没有素材字段。
- Thought Process: 利用 serde default 和现有 normalize 流程，使缺字段自然回落。
- Options Considered: 单独迁移函数；强制用户重置；字段默认值加 schema 5。
- Chosen Solution: schema 升至 5，新增字段 serde 默认空库和无绑定。
- Rationale: 旧用户继续使用内建灯组，无迁移中断。
