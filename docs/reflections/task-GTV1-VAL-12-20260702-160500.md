- Task: GTV1-VAL-12 tray 聚合状态、settings 总状态和 widget 来源状态之间无明显矛盾
- Encountered Problem: 三个展示面分别来自不同 UI 层，即使都编译通过，也可能在刷新或聚合上出现状态不一致。
- Thought Process: 需要以真实联调确认“同一次运行中三处读到的是同一份语义快照”，而不是分别单点通过。
- Options Considered: 1. 只验证单个页面。2. 只验证 tooltip。3. 同时对照 tray、settings 和 widget 的状态一致性。
- Chosen Solution: 采用人工联调结果，确认 tray 聚合状态、settings 总状态和 widget 来源状态之间无明显矛盾。
- Rationale: 这项本质是集成一致性验证，只能通过多视图对照来证明。
