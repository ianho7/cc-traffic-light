- Task: P2-04 为每次句柄探测输出错误码、窗口类名或矩形信息，确保失败可诊断
- Encountered Problem: 只有句柄没有上下文时，定位问题会落回“猜这个句柄是不是对的”。
- Thought Process: 把每个关键窗口最小但足够的诊断信息固定下来，优先记录句柄和矩形。
- Options Considered: 只打印句柄；上完整日志框架；保留最小结构化输出。
- Chosen Solution: 输出了 `Shell_TrayWnd`、`Start`、`TrayNotifyWnd`、`ReBarWindow32` 等窗口的句柄和矩形。
- Rationale: 这足以支撑当前 MVP 的排错，而且不会引入额外依赖。
