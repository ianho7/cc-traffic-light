- Task: TLU-D-04 确认真实事件驱动路径不要求修改 taskbar.rs 或状态 schema
- Encountered Problem: 真实 hook 异常曾经表现为 UI 不符合预期，容易误判成宿主路径或状态 schema 问题。
- Thought Process: 回看修复范围，确认只修改了 `hook_rules.rs` 中的事件到状态映射，未触碰 `taskbar.rs` 和 `agent_state` schema。
- Options Considered: 把问题继续扩散到 host 路径；把问题收敛到规则层。
- Chosen Solution: 收敛到规则层，并记录该验证项通过。
- Rationale: 这保持了 P3 的范围边界，也证明主路径稳定。
