- Task: AUD-6-03 设置页 hook 状态展示
- Encountered Problem: 当前实现只有 not_installed、installed、active，不能表达 Claude 进程降级。
- Thought Process: 将当前真实支持等级映射为 active、installed、not_installed、process_only；不提前虚构 ConfiguredUnverified/Error backend 状态。
- Options Considered: 一次性引入完整五态；先增加 process_only；继续把 Claude 显示为未安装。
- Chosen Solution: 先增加 process_only，并保留后续更细粒度状态模型任务。
- Rationale: 立即修复误导文案，同时避免未验证的状态语义。
- Evidence: MonitoringPage.tsx 和 shared_core HookStatus 变更，三类编译/测试通过。
- Status: completed
