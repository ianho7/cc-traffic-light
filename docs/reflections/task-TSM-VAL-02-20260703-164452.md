- Task: TSM-VAL-02 shared-core 相关 Rust tests 通过
- Encountered Problem: shared-core 新承接了配置模型、状态投影和协议层，如果没有测试证据，后续 Tauri 集成会建立在不稳定基础上。
- Thought Process: 我保留配置测试并加入状态投影测试，然后运行 `cargo test --workspace --offline`，用 workspace 级结果覆盖 shared-core 相关测试。
- Options Considered: 只跑 shared-core 单 crate；只跑旧 taskbar-widget tests；以 workspace tests 统一验证 shared-core 与宿主兼容性。
- Chosen Solution: 使用 `cargo test --workspace --offline` 作为 shared-core 测试通过的当前证据。
- Rationale: 这不仅验证 shared-core 自身，也验证它与宿主和 Tauri crate 的集成没有在测试编译路径上破掉。
