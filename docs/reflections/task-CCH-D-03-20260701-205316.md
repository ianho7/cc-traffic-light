- Task: CCH-D-03 验证 session_id_source 为 payload
- Encountered Problem: 即使出现了 Claude task，也需要确认它不是 fallback `unknown`。
- Thought Process: 检查 `taskbar_widget_hook.exe list` 中 Claude 记录的 `session_id_source` 字段。
- Options Considered: 只看 task key 名称；同时核对 `session_id_source`。
- Chosen Solution: 同时核对 `session_id_source`。
- Rationale: 这能证明真实身份字段提取已经打通，而不是靠诊断兜底。
