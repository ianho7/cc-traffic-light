- Task: GTV1-PRE-04 Confirm new module names and target file locations
- Encountered Problem: Without early naming decisions, Phase 1 could easily turn into ad hoc structs left inside main.rs.
- Thought Process: I mapped the requirement language directly to a small set of Rust modules that could later host tray, settings, detector, and aggregation logic.
- Options Considered: Keep everything in main.rs for now; create one generic app.rs module; create focused schema/contract modules first.
- Chosen Solution: Create focused modules for config, runtime contract, and UI state.
- Rationale: That gives later phases stable extension points without prematurely implementing full runtime subsystems.
