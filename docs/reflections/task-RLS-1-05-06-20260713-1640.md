- Task: RLS-1-05 至 RLS-1-06 Explorer recovery runtime verification
- Encountered Problem: Explorer restart makes the first immediate tray registration fail while the shell is still restarting.
- Thought Process: The host/PID and new widget HWND already recovered, but a failed tray registration could violate the one-tray invariant.
- Options Considered: accept first retry, add a delayed tray retry, restart the host.
- Chosen Solution: track tray registration and retry it alongside the attach retry; deploy the new root release host and rerun Explorer restart.
- Rationale: preserves a single host and never re-adds an already registered tray icon.
- Evidence: `%TEMP%\\cc-traffic-light-explorer-recovery-20260713-163739.log` shows `WM_NCDESTROY`, new HWND, `tray icon registration pending`, `tray icon registered`, and new attach; PID 6512 survived with host_count=1.
- Status: completed
