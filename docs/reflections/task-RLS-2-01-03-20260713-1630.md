- Task: RLS-2-01 至 RLS-2-03 reduced motion
- Encountered Problem: 配置模型已有字段，但 UI 和 renderer 未消费。
- Thought Process: 保留现有状态颜色与位置，以常亮替代闪烁，避免引入新的视觉语义。
- Options Considered: 隐藏状态、降低亮度、每个状态常亮。
- Chosen Solution: Appearance checkbox 持久化 `appearance.reduced_motion`；effects 在此模式固定 active alpha 且停止 animation invalidation。
- Rationale: Working/Waiting/Error 仍可由绿/黄/红位置区分，且不再闪烁。
- Evidence: `cargo test -p taskbar-widget widget_effects --offline`（5 passed）；`pnpm build`；`cargo test --workspace --offline`；installed Settings UI toggle persisted `config.json` then restored.
- Status: completed
