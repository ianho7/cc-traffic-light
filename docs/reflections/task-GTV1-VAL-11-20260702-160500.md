- Task: GTV1-VAL-11 widget 挂载失败时软件保留 tray 并后台重试恢复
- Encountered Problem: 降级恢复状态机已经实现，但此前仍缺少真实环境证据说明“挂载失败 -> tray-only -> 自动恢复”这条链路成立。
- Thought Process: 该项要验证的是运行时生命周期，不是单个函数返回值，因此需要人工构造或观察真实降级流程。
- Options Considered: 1. 只看状态机代码。2. 只看日志。3. 用人工联调确认 tray-only 保活与后台恢复都正常。
- Chosen Solution: 采用用户人工联调结果，确认 widget 挂载失败时软件会保留 tray，并在后台自动重试恢复。
- Rationale: 降级恢复是系统级行为，只有真实运行证据才能证明它真的闭环。
