- Task: AUD-VAL-10 payload fixture coverage
- Encountered Problem: payload 边界证据分散在 hook CLI tests 和隔离命令中。
- Thought Process: 对照 checklist 要求核对 session、event、编码、空输入、非法 JSON、乱序事件是否都有独立证据。
- Options Considered: 只保留 unit tests；只保留手工 state 输出；组合 unit tests 与隔离 CLI fixtures。
- Chosen Solution: 使用 hook CLI unit tests 覆盖编码/空输入，隔离 CLI 覆盖 session/event/乱序/非法 JSON。
- Rationale: 同时覆盖解析函数和真实进程边界。
- Evidence: AUD-4-01、AUD-4-02、AUD-4-04、AUD-2-07 reflections。
- Status: completed
