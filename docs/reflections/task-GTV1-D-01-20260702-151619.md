- Task: GTV1-D-01 Expand the widget into a dual-source layout for Codex and Claude
- Encountered Problem: The existing widget render path only understood a single global summary, so it could not represent per-source state even after detector aggregation existed.
- Thought Process: I changed the widget paint input to use the unified AppStatusSnapshot instead of the old HookSummary and then split the compact taskbar area into two source regions.
- Options Considered: Keep the old single-state widget longer; render dual-source data only in settings; refactor the widget now to consume the snapshot directly.
- Chosen Solution: Refactor the widget now to consume the snapshot directly.
- Rationale: The final product requires a dual-source widget, and continuing to build other runtime layers on top of a single-state widget would delay the core visible behavior.
