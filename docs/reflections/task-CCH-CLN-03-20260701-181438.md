- Task: CCH-CLN-03 确认实验配置与正式示例配置边界清晰，没有把个人本地调试文件提交到仓库
- Encountered Problem: 新增了 `.claude/settings.local.json` 作为本地实验入口，如果不显式忽略，后续很容易被误提交。
- Thought Process: 先检查现有 `.gitignore`，发现没有 `.claude` 相关规则，再补上本地 settings 与未来 hook 日志目录的忽略项。
- Options Considered: 保持现状并靠人工避免提交；把本地实验配置和日志目录加入 `.gitignore`。
- Chosen Solution: 把 `.claude/settings.local.json` 和 `.claude/hook-logs/` 加入 `.gitignore`。
- Rationale: 这符合 Claude hooks 集成文档对 local settings 和本地日志的边界建议，也能降低后续采样阶段的误提交风险。
