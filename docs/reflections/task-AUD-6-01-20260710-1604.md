- Task: AUD-6-01 Claude source key 与配置路径修正
- Encountered Problem: React 设置页使用 claude_code 查询 snapshot，而 Rust snapshot key 是 claude，导致 Claude 卡片可能一直显示 idle。
- Thought Process: 对照 shared-core SourceId、detector、Tauri types 和 MonitoringPage，统一 UI 查询 key 为 claude。
- Options Considered: 修改 Rust snapshot key；增加别名；修改 React 查询 key。
- Chosen Solution: 保持共享 Rust 合约不变，修改 MonitoringPage 的 source id 和查询 key。
- Rationale: 变更最小，不破坏现有 snapshot、widget 和 IPC 合约。
- Evidence: npx tsc -p tsconfig.app.json --noEmit 通过；cargo check -p taskbar-widget --offline 通过。
- Status: completed
