- Task: CCH-C-04 审查独立排序字段是否存在
- Encountered Problem: 需要判断是否可以用 Claude 原生事件顺序字段替代 `received_at` 兜底。
- Thought Process: 对比三类真实 sample 的 `event_order_candidates` 和 shape 内容，查找 `event_order`、`timestamp`、`created_at` 等字段。
- Options Considered: 继续等待更多事件后再判断；先基于当前真实证据确认仍需 `received_at`。
- Chosen Solution: 记录当前仍需 `received_at` 兜底。
- Rationale: 三类真实事件都没有给出独立排序字段，当前没有理由扩大字段猜测。
