- Task: CCH-VAL-02 运行 cargo build
- Encountered Problem: Claude 实验配置实际依赖 `taskbar_widget_hook.exe sample`，必须确认二进制存在并可执行。
- Thought Process: 在 `taskbar-widget/` 下运行 `cargo build`，观察是否成功产出 `target\\debug\\taskbar_widget_hook.exe`。
- Options Considered: 只跑 `cargo check`；补跑 `cargo build`。
- Chosen Solution: 补跑 `cargo build`。
- Rationale: 真实 hooks command 依赖可执行文件存在，`cargo build` 是必要基线。
