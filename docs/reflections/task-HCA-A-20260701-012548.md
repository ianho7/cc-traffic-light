- Task: HCA-A-01 through HCA-A-06
- Encountered Problem: Codex 当前可见入口是 `notify`，不是已确认的多事件 hooks TOML；实际探针需要临时修改外部用户配置。
- Thought Process: 探针方案必须先定义隐私边界、原 notify 转发要求、停机条件和低保真判定，不能直接生成或安装 wrapper。
- Options Considered: 直接修改 Codex 配置；不处理 Codex；新增只读文档让用户审阅后再执行。
- Chosen Solution: 新增 `docs/checklist/codex-notify-probe.md`，记录 wrapper shape-only 输出、转发规则、人工步骤和判定条件。
- Rationale: 文档推进了 Codex 接入策略，但实际外部配置操作仍等待用户确认，符合本轮 policy。
