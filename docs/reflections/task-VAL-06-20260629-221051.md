- Task: VAL-06 关闭程序后模块消失，再次启动能重复出现
- Encountered Problem: 如果关闭是靠强杀，模块消失的证据并不干净。
- Thought Process: 用对子窗口发送 `WM_CLOSE` 的优雅退出作为第一条证据，再用第二轮启动复现作为第二条证据。
- Options Considered: 强杀退出；人工关闭；子窗口 `WM_CLOSE` + 二次启动。
- Chosen Solution: 子窗口优雅关闭并得到 `ExitCode=0`，随后第二轮再次成功探测、挂载、定位。
- Rationale: 这证明关闭和重启都走通了。
