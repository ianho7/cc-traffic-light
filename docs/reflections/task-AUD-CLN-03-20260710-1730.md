- Task: AUD-CLN-03 实验 Claude 配置不进入默认安装
- Encountered Problem: Claude 示例文件存在于仓库中，必须确认它不会被 installer 或首次运行流程自动部署。
- Thought Process: 以 installer.iss 的 Files/Run 清单和安装脚本调用关系为准，不以文件是否存在于仓库推断默认安装行为。
- Options Considered: 删除 Claude 示例；将其加入安装器；保留示例但明确只作为实验配置并不自动部署。
- Chosen Solution: 保留实验示例供人工验证，installer 只复制三个 exe、Codex 脚本和资源，不复制或写入 `.claude/settings.json`。
- Rationale: 符合当前 Claude command hook 不稳定的产品限制，避免安装器产生过度承诺。
- Evidence: `installer.iss` 的 `[Files]`/`[Run]`；`scripts/pack-all.ps1`；`examples.claude-hooks.json` 未出现在安装器动作中。
- Status: completed
