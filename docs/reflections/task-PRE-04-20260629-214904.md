- Task: PRE-04 确认唯一核心依赖为 `windows` crate，并列出首批 Win32 API 清单
- Encountered Problem: 如果在 Phase 1 就引入额外 GUI 或日志依赖，会把问题空间从 Win32 基线扩大到工具链集成。
- Thought Process: 严格对照计划文档，把依赖和 API 范围压到实现普通窗口与绘制所必需的最小集合。
- Options Considered: 加 GUI 框架；加日志框架；只使用 `windows = 0.58` 并记录必须 API。
- Chosen Solution: 只添加 `windows 0.58`，并在 preflight 文档中记录窗口创建、消息循环和绘制路径的核心 API。
- Rationale: MVP 要验证的是底层能力，不是框架选型。
