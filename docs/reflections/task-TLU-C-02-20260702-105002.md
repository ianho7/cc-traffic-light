- Task: TLU-C-02 用 debug set 验证 `working` 的颜色、形态和可读性
- Encountered Problem: 需要确认 `working` 是否会被渲染成紧凑的蓝色运行态，而不是回退到旧文本样式。
- Thought Process: 在隔离状态中用单任务 `working` 触发 UI，观察状态灯、标签和计数是否同时正确。
- Options Considered: 继续用历史混合状态观察；隔离后只测单任务 `working`。
- Chosen Solution: 隔离状态下执行 `set codex_123 working`。
- Rationale: 这样能把 `run` 蓝色 `1` 直接归因到当前绘制映射，而不是其他任务干扰。
