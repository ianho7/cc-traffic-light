- Task: CCH-A-03 使用真实 matcher group 结构创建实验配置
- Encountered Problem: 现有仓库示例不是官方文档要求的三层结构，直接沿用风险很高。
- Thought Process: 按 `hooks -> Event -> matcher group -> hooks` 结构创建新配置，并为每个事件显式设置空 matcher。
- Options Considered: 平面 `type/command`；matcher group 结构。
- Chosen Solution: matcher group 结构。
- Rationale: 这是与集成说明一致的最小真实配置形态。
