- Task: TLU-B-04 保留最小必要文本而不回退到旧样式
- Encountered Problem: 完整 `WORKING 1` 这类文本太长，会抢掉图形空间。
- Thought Process: 用更短的状态词和可选计数，仅在需要时显示额外字符。
- Options Considered: 保留完整状态词；只显示计数；显示紧凑状态词和可选计数。
- Chosen Solution: 显示紧凑状态词和可选计数。
- Rationale: 这样保住了语义密度，又避免恢复成文字主导界面。
