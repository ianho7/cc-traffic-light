- Task: CCH-B-02 触发真实 PreToolUse shape-only sample
- Encountered Problem: 需要确认 Claude Code 在工具调用前的 payload 是否包含 `tool_name`、`tool_input` 和 `tool_use_id` 等关键字段。
- Thought Process: 触发一次读文件工具调用，再读取 `.claude/hook-logs/PreToolUse.jsonl`。
- Options Considered: 只依赖 `/hooks` 事件列表；读取真实 PreToolUse sample。
- Chosen Solution: 读取真实 PreToolUse sample。
- Rationale: 这样可以证明 Claude 的工具前事件足够支撑共享状态和后续调试，而不只是“理论上有这个事件”。
