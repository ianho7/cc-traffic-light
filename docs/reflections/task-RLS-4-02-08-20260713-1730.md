- Task: RLS-4-02 至 RLS-4-08 Claude support level
- Encountered Problem: command execution depended on shell form; initial dump wrapper read stdin incorrectly.
- Thought Process: isolate marker first, fix only the wrapper, then validate JSON shape, event sequence, and state write using the same PowerShell form.
- Options Considered: require all shell forms, retain ProcessOnly, support only PowerShell shell form.
- Chosen Solution: Active support is limited to PowerShell shell form on Claude Code 2.1.204 / Windows 11; other forms/events are accepted limitations.
- Rationale: this is the user-approved production requirement and has direct trigger, stdin, state, and Stop evidence.
- Evidence: shape-only dumps in `%TEMP%\\cc-traffic-light-claude-hooks`; `state.json` latest validated Claude task ended `Stop -> done`.
- Status: completed_with_accepted_limitation
