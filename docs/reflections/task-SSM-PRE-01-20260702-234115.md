- Task: SSM-PRE-01 阅读迁移计划并确认边界、持久化和 i18n 范围
- Encountered Problem: 需要先确认 Slint 迁移是否只替换 settings，避免后续把 widget 或 tray 一起拖入重构。
- Thought Process: 对照迁移计划中的 objective、scope、non-goals 和 validation，提炼出实际执行边界。
- Options Considered: 保持边界口头约定；把边界显式写进 checklist 和 plan。
- Chosen Solution: 采用文档显式边界，以 checklist 与 plan 双份约束 Win32 主体不动、Slint 只接管 settings。
- Rationale: 这能在真正接入 Slint 前先锁住范围，避免 UI 重构蔓延到高风险的 taskbar attach 路径。
