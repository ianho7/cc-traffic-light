- Task: CCH-B-04 触发真实 Stop shape-only sample
- Encountered Problem: 在进入真实状态写入前，必须确认 Claude Code 的结束态事件也携带 `session_id` 和 `hook_event_name`。
- Thought Process: 重新把 `Stop` 挂回 sample 配置，触发一次正常结束对话，再读取 `.claude/hook-logs/Stop.jsonl`。
- Options Considered: 直接跳到真实状态写入；先补真实 Stop 证据。
- Chosen Solution: 先补真实 Stop 证据。
- Rationale: 这样 `done` 路径的状态映射才有真实结束态 payload 作为依据。
