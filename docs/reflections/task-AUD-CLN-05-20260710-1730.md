- Task: AUD-CLN-05 命名与 source key 清理
- Encountered Problem: checklist 旧文案仍写 Installed，MonitoringPage 通过显示文本而非 canonical key 查找 Claude label，容易掩盖 key 漂移。
- Thought Process: 后端和状态文件统一输出 `claude`；旧 `claude_code` 只作为输入兼容 alias，不应继续成为新 DTO 或页面调用的 key。
- Options Considered: 删除所有 alias；继续混用两个 canonical key；统一新代码并保留明确注释的兼容入口。
- Chosen Solution: MonitoringPage 使用 `sourceLabel("claude")`，shared parser 和 label lookup 明确 legacy alias 仅用于兼容，更新 checklist 文案。
- Rationale: 不破坏旧快照，同时保证新链路只有一个 canonical source key。
- Evidence: `ui_state.rs`、`label-lookup.ts`、`MonitoringPage.tsx`；TypeScript 检查和 workspace Rust tests 通过。
- Status: completed
