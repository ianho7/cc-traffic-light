- Task: HCA-B-GATE, HCA-B-01 through HCA-B-06
- Encountered Problem: `taskbar_widget_hook.rs` 同时承担 CLI、payload 提取、状态推断和采样输出，后续适配真实 payload 会扩大修改面。
- Thought Process: 规则层应变成纯函数模块，CLI 只保留输入/输出和状态写入编排，这样可以局部调整 hook 语义。
- Options Considered: 保持现状；新增规则模块但只移动映射；完整移动映射、Stop heuristic、字段提取和 payload shape。
- Chosen Solution: 新增 `taskbar-widget/src/hook_rules.rs`，并通过 `lib.rs` 导出；CLI 调用 `hook_rules` 完成规则判断。
- Rationale: 拆分未改变 state schema、状态路径和 Codex/Claude 成功输出语义，`cargo check` 已验证通过。
