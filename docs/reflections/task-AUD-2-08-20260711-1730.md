- Task: AUD-2-08 Codex 真实 lifecycle
- Thought Process: 先验证 state 缺失是否由 hook 退出码导致，再解析真实 hooks.json 中的 command 路径，并用 Test-Path 验证目标 exe。
- Encountered Problem: Codex 的 UserPromptSubmit、PreToolUse、PermissionRequest、PostToolUse、Stop 等 hook 均 exited with code 1，且 `$env:TASKBAR_WIDGET_STATE_HOME\state.json` 不存在。
- Root Cause: 真实用户 hooks.json 仍保留旧版本路径 `%LOCALAPPDATA%\CcTrafficLight\bin\taskbar_widget_hook.exe`；该文件不存在。当前 install-codex-hooks.ps1 与 installer.iss 的正式路径是 `%LOCALAPPDATA%\Programs\CC Traffic Light\taskbar_widget_hook.exe`。
- Evidence: `Test-Path C:\Users\admin\AppData\Local\CcTrafficLight\bin\taskbar_widget_hook.exe = False`；hooks.json 中所有 commandWindows 均指向该不存在路径；BOM 已修复且 `/hooks` 已能继续解析配置。
- Chosen Solution: 先完成正式安装或用正式安装路径部署 release hook，再重新运行 hooks installer 更新 hooks.json；重启 Codex 后重新执行 `/hooks` review/trust 和只读任务。
- Remaining Risk: 在真实 exe 部署前，不能判断 Codex payload、trust 或 state 写入逻辑是否还有第二个问题。
- Follow-up 2026-07-13: 正式安装路径下的 exe 与 installer script 均存在；真实 hooks.json 的 7 个 commandWindows 都已改为该路径。向 release exe 重放 `UserPromptSubmit` JSON 后，隔离 state.json 中成功出现 `codex_codex-direct-verification`，状态为 `working`。剩余风险收敛为真实 Codex 进程的环境变量继承、hook trust 和 lifecycle 触发。
- Follow-up 2026-07-13 (real CLI): trust hashes 已记录在 `%USERPROFILE%\\.codex\\config.toml`，但在手工创建的空 `$evidence\\codex-project` 中运行 Codex CLI 0.144.1，所有 hooks code 1。其同一 session 的普通 `Get-ChildItem` 也失败，sandbox log 显示 CLI 以裸名称启动 `codex-windows-sandbox-setup.exe`，报 program not found。后续 `git init` 后故障仍复现，因此“非 Git CWD 是根因”的判断被证伪。下一步应在实际仓库中把隔离 state 放入 workspace 可写目录，分离临时目录 sandbox 故障与 hook 对 state 路径的写入权限问题。
- Follow-up 2026-07-13 (stdout fix): 在实际仓库、禁用项目级重复 hooks 后，用户级 release hook 仍显示 code 1，但同一会话的真实 `Stop` 已写出 `codex_<session_id>=done`。这证明错误发生在 `apply_hook_event` 之后。hook CLI 原先只对 Codex 返回 `{}`，从而执行 `println!`; Windows hook runner 的 stdout 不可写时会在 state 写入后 panic，形成“state 成功但 UI code 1”。改为所有成功 hook 都返回空 stdout；官方 Codex 文档也规定 exit 0 且无输出为成功。`cargo test -p taskbar-widget --bin taskbar_widget_hook --offline` 通过（4/4）；release hook 已替换到正式安装路径，直接 payload 回放验证 exit 0、无 stdout/stderr、state 成功写入。等待真实 CLI 复验。
