- Task: TSM-PRE-03 冻结 UI fidelity 基线
- Encountered Problem: 迁移到 Tauri 后最容易出现“技术上换了栈，但视觉基线漂移”的问题。
- Thought Process: 我把 HTML demo、spec 和 checklist 一起看，而不是只看 demo，避免把演示交互误当成正式需求。
- Options Considered: 只参考 HTML；只参考 spec；把 HTML、spec、checklist 组合成统一 fidelity 基线。
- Chosen Solution: 采用 `cc_traffic_light_nothing_demo_strict.html` + `nothing-signal-console-spec.md` + `nothing-signal-console-checklist.md` 作为联合基线。
- Rationale: 这样既能保住视觉方向，也能明确哪些交互不能被 HTML demo 倒逼新增。
