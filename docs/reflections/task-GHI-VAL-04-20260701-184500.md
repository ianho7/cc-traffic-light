- Task: GHI-VAL-04 validate fixture apply backup and merge behavior
- Encountered Problem: apply 既可能漏备份，也可能错误覆盖无关 hooks。
- Thought Process: 重点看 unrelated fixture：它最能暴露“并存是否被破坏”。
- Options Considered: 只看 empty fixture；只检查 backup 文件存在；同时检查 backup 和 merged entry count。
- Chosen Solution: 在 unrelated fixture 上确认 backup 生成且 `PreToolUse` 变成 2 条并存。
- Rationale: 这直接证明 install script 没有吞掉原有条目。
