- Task: CSW-B-02 switch project hooks to taskbar_widget_hook.exe
- Encountered Problem: 当前项目 hooks 仍只做 shape dump，无法推动 P0 的真实状态写入目标。
- Thought Process: 在不改变事件集合和 matcher 结构的前提下，只替换 command / commandWindows 的目标路径。
- Options Considered: 保持 dump 模式；写一个 dump+write wrapper；直接切换到 `taskbar_widget_hook.exe codex <HookName>`。
- Chosen Solution: 直接将每个 lifecycle event 的命令切到 `taskbar_widget_hook.exe codex <HookName>`。
- Rationale: 这与 P0 计划完全一致，改动面最小，也不会顺带引入新的持久化或隐私面。
