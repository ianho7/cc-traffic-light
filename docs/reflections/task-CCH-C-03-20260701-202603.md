- Task: CCH-C-03 确认真实 payload 中存在 hook_event_name
- Encountered Problem: 如果真实 payload 没有 hook 名字段，状态写入逻辑只能长期依赖 argv 兜底。
- Thought Process: 查看 sample 中的 `hook_name_candidates`，确认是否真实命中 `$.hook_event_name`。
- Options Considered: 继续假设 argv 兜底；根据真实 sample 确认 `hook_event_name` 可直接命中。
- Chosen Solution: 确认 `hook_event_name` 为真实字段。
- Rationale: 这意味着当前 parser 的 hook 名候选字段可直接复用，不需要为 Claude 特判。
