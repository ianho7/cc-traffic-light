- Task: PRE-07 约定反射文档目录为 `docs/reflections/`，任务完成后按 task id 生成记录
- Encountered Problem: checklist 要求每个完成项自动生成反射，如果目录和命名方案不先固定，记录会很快失控。
- Thought Process: 直接按 `task-<task-id>-<timestamp>.md` 创建文件，让文档结构和 checklist ID 一一对应。
- Options Considered: 不写反射；写一个总日志；按 checklist ID 拆分成独立反射文件。
- Chosen Solution: 创建 `docs/reflections/` 并为已完成任务逐项生成反射文件。
- Rationale: 这样后续可以从 checklist 直接追到每个决策和问题，不需要再做额外索引。
