- Task: GTV1-B-04 Implement source-card skeletons for Codex and Claude
- Encountered Problem: The source cards needed to show product-relevant metadata now, even though detector-specific timestamps and richer diagnostics are not implemented yet.
- Thought Process: I rendered the stable subset first: state, detection method, and confidence, and left timing/detail expansion for later detector work.
- Options Considered: Wait until detector implementation is done; render cards with placeholders only; render cards from the snapshot with the fields already available.
- Chosen Solution: Render cards from the snapshot with the fields already available.
- Rationale: It gives the settings overview a real data contract immediately and avoids fake UI that later diverges from runtime state.
