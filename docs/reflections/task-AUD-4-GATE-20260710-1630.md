- Task: AUD-4-GATE Hook CLI、状态模型与 detector 证据门
- Encountered Problem: Phase 4 初始只覆盖了部分状态分类和 detector 逻辑，且 status DTO 不能表达未验证与损坏。
- Thought Process: 先用 fixture/单测覆盖事件顺序、并发、编码、损坏恢复、TTL、优先级和 fallback precedence，再补齐 DTO 状态语义。
- Options Considered: 只保留已有单测；把剩余状态留给手工验收；完成自动化边界并明确把真实 Agent trust 留到 Phase 2/3。
- Chosen Solution: 完成 Phase 4 的自动化与 fixture 证据门，新增 ConfiguredUnverified/Error，并保留真实 Codex/Claude 触发作为外部验收项。
- Rationale: Phase 4 可以独立证明状态层的确定性，不把未完成的真实客户端验证伪装成状态层完成。
- Evidence: `cargo test --workspace --offline` 通过；Phase 4 相关 12 个 host tests、4 个 hook CLI tests、detector/widget tests 均通过；并发/TTL/损坏恢复 fixture 已记录在对应 reflections。
- Status: completed
