- Task: GTV1-VAL-06 Codex detector 至少命中一条有效来源，并给出合理可信度
- Encountered Problem: detector 代码虽然已经实现多来源优先级和 process fallback，但此前没有人工联调证据说明 `Codex` 在用户环境中确实会命中有效来源。
- Thought Process: 这项验证的重点不是“命中了哪一种实现”，而是“真实环境中至少存在一条可用来源，且可信度没有明显失真”。
- Options Considered: 1. 只看 detector 实现。2. 只看 tooltip。3. 以真实 `Overview` / `Diagnostics` 联调结果确认来源命中与可信度合理。
- Chosen Solution: 采用人工联调结果，确认 `Codex` detector 已命中至少一条有效来源，并给出合理可信度。
- Rationale: detector 是否真的能在用户机器上工作，必须以真实运行结果而不是代码意图为准。
