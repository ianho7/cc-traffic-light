- Task: TLU-PRE-05 确认本轮验证命令集合
- Encountered Problem: P3 既有代码层验证，也有桌面可见性验证，验证入口不统一容易导致自判通过。
- Thought Process: 先确认最小可重复命令集，覆盖格式、编译、启动和人工状态注入。
- Options Considered: 只用 `cargo check`；建立包含 `cargo fmt -- --check`、`cargo check`、`cargo run`、`taskbar_widget_hook.exe set` 的最小集合。
- Chosen Solution: 采用后者作为 checklist 的基线 verifier。
- Rationale: 这套命令能分别覆盖代码质量、编译可用性、运行可见性和状态切换。
