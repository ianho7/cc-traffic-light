# RLS-6-03 至 RLS-6-04：最终恢复与质量验证

- Encountered Problem: 最终 gate 必须使用单独 release 构建，并且格式检查不能以修改用户既有 dirty 文件为代价。
- Chosen Solution: 复用实际 Explorer recovery runtime log，运行 workspace tests、frontend build 与 settings→host 顺序的独立 release builds；只读执行格式检查。
- Evidence: Explorer log `%TEMP%\cc-traffic-light-explorer-recovery-20260713-163739.log`; `cargo test --workspace --offline`; `pnpm build`; separate release builds all passed.
- Limitation: `cargo fmt --all -- --check` reported existing formatting differences. No formatting write was performed.
- Status: completed with an explicit non-mutating formatting limitation.
