- Task: CSW-C-07 confirm `session_id_source = payload`
- Encountered Problem: 即使出现了真实 task，也必须排除 fallback `unknown` 这种弱通过。
- Thought Process: 观察正式 list 输出里的每个真实 task 字段，确认 session 提取来源而不是只看 task key 字符串。
- Options Considered: 只按 task key 名字推断；重新查 payload 采样；直接核对 `session_id_source` 字段。
- Chosen Solution: 以 list 输出中的 `session_id_source = payload` 作为完成证据。
- Rationale: 这让“真实会话 ID 提取成功”成为显式字段判断，而不是间接猜测。
