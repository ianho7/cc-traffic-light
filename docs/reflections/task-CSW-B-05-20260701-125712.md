- Task: CSW-B-05 record that hook changes require a new Codex trust review
- Encountered Problem: `.codex/hooks.json` 从 dump 脚本切到真实状态写入后，下一轮失败最可能来自 trust 没刷新，而不是代码本身。
- Thought Process: 把“需要重新 review/trust”明确写进 checklist 和 handoff，避免后续把零触发误判成实现回归。
- Options Considered: 只在对话里口头说明；写进 checklist 注记；等真实会话失败后再补文档。
- Chosen Solution: 立即把 trust 作为后续人工前置条件写入 checklist 与 handoff。
- Rationale: 这能把剩余外部 gate 单独暴露出来，减少后续排障时把配置 trust 和代码实现混在一起。
