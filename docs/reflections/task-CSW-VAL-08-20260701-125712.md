- Task: CSW-VAL-08 manually verify the widget reflects the updated summary without restart
- Encountered Problem: checklist 要求人工观察 widget summary 是否变化，不能只依赖内部 state 或 hook CLI 输出。
- Thought Process: 通过 debug CLI 依次写入 `working`、`waiting`、`done`，并在 widget 运行中抓取任务栏截图。
- Options Considered: 只用手工肉眼看；只看 stdout；截图加 stdout 双证据。
- Chosen Solution: 采用截图和 stdout 双证据，确认无需重启即可看到 `IDLE/WORKING 1/WAITING 1/DONE 1`。
- Rationale: 这已经足以证明 widget 的本地重绘链路成立，即使它还不是由真实 Codex 事件驱动的。
