- Task: HC-P0-01, HC-P0-02, HC-P0-05, HC-P0-06
- Encountered Problem: 本轮没有真实 Claude Code / Codex hook 环境，无法确认所有真实 payload 是否携带 `session_id` 和事件顺序字段。
- Thought Process: 按用户确认的选项 2，将真实采样缺口记录为 accepted limitation，同时实现 `sample` 模式保证后续接真实 hook 时可以脱敏观察字段结构。
- Options Considered: 暂停等待真实 payload；直接猜测字段并继续；实现 shape-only 采样并用人工 payload 推进。
- Chosen Solution: 实现 `taskbar_widget_hook.exe sample`，只输出候选字段路径和值类型；采样结论写入 `docs/checklist/hook-payload-sampling.md`。
- Rationale: 这保留了 Phase 0 gate 的风险记录，同时不阻塞状态模型、CLI 和 widget 消费路径的最小闭环。
