- Task: HCA-VAL-01 through HCA-VAL-10
- Encountered Problem: 普通沙箱下 `cargo build` 写 `target\debug\.cargo-lock` 被拒绝，需要提升权限生成新版 hook CLI 后才能验证 binary 行为。
- Thought Process: `cargo check` 足以验证编译，但人工 CLI 回归必须使用更新后的 exe，因此按策略请求提升权限构建。
- Options Considered: 只跑 `cargo check`；使用旧 exe；提升权限构建后跑完整人工断言。
- Chosen Solution: `cargo check` 通过后，用已审批 `cargo build` 生成 binary，并在临时状态目录验证 working、waiting、error、Stop waiting、SubagentStop、未知 hook、缺失 session、乱序、Codex `{}` 和 Claude 静默。
- Rationale: 回归覆盖了 checklist 的规则行为和输出兼容要求，验证目录位于 `%TEMP%`，没有写入真实 appdata。
