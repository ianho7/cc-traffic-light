- Task: CCH-C-01 确认 Claude 真实 payload 存在 session_id
- Encountered Problem: P2 的核心风险是 Claude 可能缺少稳定身份字段，导致只能写入 `claude_unknown`。
- Thought Process: 对比 `UserPromptSubmit`、`PreToolUse`、`PostToolUse` 三类真实 sample，检查 `session_id_candidates` 和 shape 中的字段路径。
- Options Considered: 保持“待确认”状态；根据真实 sample 收敛为已确认。
- Chosen Solution: 根据真实 sample 将 `session_id` 收敛为已确认字段。
- Rationale: 三类真实事件都直接命中了 `$.session_id`，这已经满足最小状态写入前提。
