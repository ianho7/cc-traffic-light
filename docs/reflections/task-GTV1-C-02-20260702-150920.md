- Task: GTV1-C-02 Implement a fixed-priority detector skeleton for Claude Code
- Encountered Problem: The product requirement treats Codex and Claude as separate monitored sources, but the runtime needed to preserve a symmetrical structure without pretending both already have full source coverage.
- Thought Process: I reused the same detector framework for both source IDs and kept currently unsupported sources as empty priority slots.
- Options Considered: Implement Codex only first; hardcode Claude as unavailable; define the same detector structure for both sources and let unimplemented sources remain empty.
- Chosen Solution: Define the same detector structure for both Codex and Claude.
- Rationale: It keeps the aggregation contract stable and avoids a later refactor when Claude-specific sources are added.
