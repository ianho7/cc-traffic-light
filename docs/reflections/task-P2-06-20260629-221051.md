- Task: P2-06 当前机器结构已记录为实际路径，并已按真实结构更新实现
- Encountered Problem: 当前机器除了经典窗口链路，还暴露了 `DesktopWindowContentBridge` 和 `InputSite` 这类现代节点。
- Thought Process: 既要记录这些差异，也要避免被它们带偏到过度建模。
- Options Considered: 忽略差异；围绕现代节点重写一套策略；记录差异但仍保持 MVP 路线最短。
- Chosen Solution: 在运行时映射文档中记录真实路径，同时继续采用 `Shell_TrayWnd + TrayNotifyWnd` 的最短实现。
- Rationale: 这满足“结构差异已记录”的要求，同时不破坏 MVP 边界。
