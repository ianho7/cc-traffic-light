- Task: CSW-A-03 verify sample mode starts
- Encountered Problem: 仅有二进制存在还不够，需要确认 CLI 至少能启动、读 stdin 并返回 shape-only 输出。
- Thought Process: 用最小 JSON 通过管道喂给 `sample` 子命令，验证 argv 解析和 JSON decode 基本路径。
- Options Considered: 只运行 `--help`/无参；运行 `sample` 空输入；运行 `sample` 最小有效 JSON。
- Chosen Solution: 对 `sample` 提供最小 JSON，检查候选字段路径和脱敏 shape 输出。
- Rationale: 这样既验证 CLI 可启动，也验证了 P0 依赖的 payload 采样路径仍然可用，且没有重新暴露原始值。
