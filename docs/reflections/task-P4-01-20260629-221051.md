- Task: P4-01 读取任务栏根窗口与所选锚点窗口的矩形
- Encountered Problem: 只有父窗口和锚点的 screen rect 同时存在，才谈得上子窗口相对定位。
- Thought Process: 直接把 `Shell_TrayWnd` 和 `TrayNotifyWnd` 的矩形纳入运行结果，而不是临时打印一次就丢掉。
- Options Considered: 只读取父窗口；只读取锚点；同时读取两者。
- Chosen Solution: 同时记录了 `parent_rect` 和 `anchor_rect`。
- Rationale: 这为后续定位公式提供了必需输入，也为运行文档提供了原始数据。
