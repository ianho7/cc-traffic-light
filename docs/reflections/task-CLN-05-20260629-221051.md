- Task: CLN-05 确认仓库中没有无意义的调试垃圾文件，并把新增文档链接收敛为仓库内相对路径
- Encountered Problem: 我曾生成过一张错误层级的屏幕拷贝图，而且早期文档里用了不少绝对路径链接。
- Thought Process: 删掉无用截图，只保留真正的任务栏证据图，同时把我新建文档里的链接改成仓库内相对路径。
- Options Considered: 保留所有中间产物；删除证据图；删除无用图并保留有效证据。
- Chosen Solution: 删除了错误截图，保留 `taskbar-phase4-printwindow.png`，并把新增文档链接改成相对路径。
- Rationale: 这让仓库产物更干净，也减少了机器私有路径扩散。
