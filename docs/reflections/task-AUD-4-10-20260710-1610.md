- Task: AUD-4-10 状态语义与 HookStatus DTO
- Encountered Problem: 原实现把配置存在但没有近期事件的状态称为 Installed，无法区分“已配置但未证明触发”；配置或共享 state JSON 损坏也没有独立状态。
- Thought Process: 状态分类必须反映证据强度，而不是把配置文件存在等同于 hook 已工作。Claude 无 command hook 证据时仍保留 ProcessOnly。
- Options Considered: 继续复用 Installed；只在前端改文案；扩展共享 DTO 并在 host 端识别 JSON 损坏。
- Chosen Solution: 增加 ConfiguredUnverified 和 Error，并让 host 在 hooks/state JSON 无法解析时返回 Error。
- Rationale: 设置页和启动通知可以据此给出可行动诊断，避免“已安装”造成过度承诺。
- Evidence: `cargo test -p taskbar-widget --bin taskbar-widget settings_bridge::tests --offline`，6 tests passed；`cargo check -p taskbar-widget --offline` passed。
- Status: completed
