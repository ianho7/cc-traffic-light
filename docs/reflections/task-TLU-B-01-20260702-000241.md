- Task: TLU-B-01 提炼状态到视觉样式的映射
- Encountered Problem: 旧版 `paint_style` 只返回字符串和前后景色，不足以表达新布局。
- Thought Process: 把标签、背景、前景、状态灯颜色和 stale 告警色归到一个结构里。
- Options Considered: 继续返回元组；引入 `PaintStyle` 结构。
- Chosen Solution: 引入 `PaintStyle` 结构。
- Rationale: 这让布局与状态映射分离，后续验证和调整更集中。
