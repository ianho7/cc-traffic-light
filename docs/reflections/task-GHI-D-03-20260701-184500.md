- Task: GHI-D-03 run apply against fixtures
- Encountered Problem: dry-run 只能证明计划，不能证明 backup 和实际写盘结果。
- Thought Process: 需要在可丢弃的 fixture 上执行真实 `apply`，再检查输出文件。
- Options Considered: 跳过 apply；直接对真实用户配置 apply；先对 fixture apply。
- Chosen Solution: 先在 fixture 上执行 `apply`，检查 backup 和 merge 结果。
- Rationale: 这样能把写盘语义和真实用户风险隔离开。
