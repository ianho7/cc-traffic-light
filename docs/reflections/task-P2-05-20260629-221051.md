- Task: P2-05 运行探测版程序，确认能稳定打印非空任务栏根句柄与所选参考窗口信息
- Encountered Problem: 需要的不只是“代码里写了 FindWindowW”，而是实际运行时在交互桌面上真能找到这些窗口。
- Thought Process: 用提权运行收集真实 stdout，把句柄、矩形和选定的 parent/anchor 一次性抓全。
- Options Considered: 只依赖静态代码审查；只看单个句柄；跑一次完整探测日志。
- Chosen Solution: 运行程序并确认 `Shell_TrayWnd`、`Start`、`TrayNotifyWnd`、`ReBarWindow32` 等信息全部非空输出。
- Rationale: 这给 Phase 2 提供了真正的运行证据，而不是推测。
