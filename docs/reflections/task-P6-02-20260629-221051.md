- Task: P6-02 记录最小运行步骤：构建、运行、观察、退出、再运行
- Encountered Problem: 如果只有代码没有运行入口，别人接手时仍然不知道怎么重复验证。
- Thought Process: 把最短命令路径和验证动作写到项目 README。
- Options Considered: 只保留 `cargo` 默认习惯；写很长的操作手册；写最小闭环步骤。
- Chosen Solution: 在 README 中记录 `cargo check`、`cargo build`、`cargo run` 以及当前验证目标。
- Rationale: 这已足够支持下一次本机复现。
