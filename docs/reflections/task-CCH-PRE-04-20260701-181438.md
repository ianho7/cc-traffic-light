- Task: CCH-PRE-04 审查 hook CLI 与字段提取实现
- Encountered Problem: 需要确认现有 Rust 路径是否已经支持 `claude` agent 和 sample/state write 两条路径。
- Thought Process: 读取 `taskbar_widget_hook.rs` 和 `hook_rules.rs`，核对 `sample`、`claude <HookName>`、`session_id` 候选字段和 `received_at` 兜底排序逻辑。
- Options Considered: 不检查源码直接配置 hooks；先核对源码能力再配置。
- Chosen Solution: 先核对源码能力。
- Rationale: 可以避免后续把配置问题误判成 hook CLI 能力缺失。
