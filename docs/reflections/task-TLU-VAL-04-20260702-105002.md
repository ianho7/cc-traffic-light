- Task: TLU-VAL-04 验证 idle/working/waiting/done/error 是否一眼可区分
- Encountered Problem: 状态切换正确不一定代表视觉辨识度足够。
- Thought Process: 以隔离状态下的五种状态为基线，检查颜色、标签和计数是否能快速区分。
- Options Considered: 只验证状态值更新；同时验证视觉区分度。
- Chosen Solution: 按五种状态逐条记录观察并据此勾选验证项。
- Rationale: P3 的目标不是仅仅“状态变了”，而是“任务栏上一眼能看懂”。
