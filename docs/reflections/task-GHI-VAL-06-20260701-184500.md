- Task: GHI-VAL-06 validate real-user dry-run summary
- Encountered Problem: P1 若没有真实用户配置摘要，下一步是否需要“合并”还是“初始化”都不清楚。
- Thought Process: 真实 dry-run 的目标不是写盘，而是收集风险边界。
- Options Considered: 等真实 apply 时再看；手工读取用户文件；让脚本自己输出脱敏摘要。
- Chosen Solution: 用脚本对真实用户路径执行 dry-run，并记录 `config_existed = false`。
- Rationale: 这让后续真实 apply 可以明确按首次安装路径准备，而不是继续假设存在旧配置。
