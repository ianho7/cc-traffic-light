- Task: GTV1-A-05 Record what stays in main.rs versus new modules
- Encountered Problem: Splitting too aggressively would risk breaking the stable Win11 startup path, while splitting too little would leave no place to grow V1.
- Thought Process: I kept the existing Win32 bootstrap, attach, timer, and paint flow in main.rs and moved only schemas and contracts into new modules.
- Options Considered: Large refactor of main.rs now; no refactor at all; keep main.rs stable and extract only Phase 1 contracts.
- Chosen Solution: Keep main.rs as the host runtime entry point and extract config/runtime/ui contracts only.
- Rationale: That preserves the current stable path while still making the codebase ready for the next phases.
