- Task: TSM-DOC-03 更新 settings 设计文档，记录与 HTML demo 的差异点和理由
- Encountered Problem: checklist 里这个文档项仍未勾选，但仓库实际上已经存在一份针对 HTML demo 差异与理由的视觉记录，如果不回写状态，就会让文档完成度被低估。
- Thought Process: 我先复核 `tauri-settings-visual-fidelity-pass-1.md` 是否真的覆盖了“哪些项对齐、哪些项未对齐、为什么允许保留差异”这三个问题，再决定是否可作为当前文档证据。
- Options Considered: 1. 重新写一份重复文档。2. 保留未勾选。3. 认可现有视觉对照记录并回写 checklist。
- Chosen Solution: 复核后确认现有文档已覆盖布局、控件形态、未对齐项和工程取舍，因此将 `TSM-DOC-03` 标记为完成。
- Rationale: 这避免重复造文档，同时让 checklist 反映当前仓库的真实文档状态。
