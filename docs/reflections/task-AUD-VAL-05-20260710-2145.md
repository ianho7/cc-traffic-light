- Task: AUD-VAL-05 Tauri settings release build
- Encountered Problem: 无。
- Thought Process: 按仓库约束先构建 Tauri settings，再构建 hook CLI 和 host。
- Options Considered: workspace-wide build；单独构建 settings；跳过 release build。
- Chosen Solution: 使用 cargo build -p taskbar-settings-tauri --release --offline。
- Rationale: 保持 host acceptance dependency graph 隔离。
- Evidence: scripts/pack-all.ps1 -SkipFrontend 成功，target/release/taskbar-settings-tauri.exe 已生成。
- Status: completed
