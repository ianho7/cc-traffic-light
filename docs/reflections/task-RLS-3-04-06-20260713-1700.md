# RLS-3-04 至 RLS-3-06：Settings 真实操作与失败 UX

- Encountered Problem: 仅有 lifecycle verifier 与 unit test 时，无法证明安装版 Settings 的按钮实际穿透 IPC 并反馈可诊断结果。
- Thought Process: 在已安装的 release Settings 中直接执行重新部署与立即刷新，再将 UI 返回、最新状态和诊断字段与既有失败 fixture 对齐。
- Chosen Solution: 「重新部署 Codex hooks」返回 `hooks deployed successfully`；「立即刷新检测」返回「检测刷新已请求」。刷新后的页面显示 Codex 已就绪、`state_file / confirmed`、最近事件、`attached` 和「最近错误：无」。
- Rationale: 这同时覆盖正常操作的可用性、状态解释和 Settings 诊断回读，而非只验证后端函数可编译。
- Failure UX Evidence: Claude 页面保留 ProcessOnly 说明；`settings_bridge` 的七个分类测试覆盖 ConfiguredUnverified、ProcessOnly 和 Error；attach failure 有 tray-only runtime log；state 不可写有正式 hook 的 `os error 183` fixture。
- Validation: `cargo test -p taskbar-widget settings_bridge --offline`（7/7 通过）；已安装 Settings 真实 IPC 操作于 2026-07-13 17:00 完成。
- Remaining Risk: Claude 的 PowerShell-only Active 支持等级及用户级部署边界已在 RLS-4 单独记录；安装器/清洁环境验收仍是用户接受的 limitation，不能由本条替代。
