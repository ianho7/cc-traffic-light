- Task: P2-02 在当前 Win11 机器上枚举并记录最小必要子窗口路径，优先围绕 `TrayNotifyWnd`、`Start` 等锚点验证结构
- Encountered Problem: 不能继续假设旧文档里的窗口树在这台机器上完全一致，必须拿到真实结构。
- Thought Process: 先抓 `Shell_TrayWnd` 的子树，再筛出对布局和宿主选择真正有价值的窗口。
- Options Considered: 直接照抄旧结构；只找一个 `Shell_TrayWnd` 就继续；枚举当前机器的真实关键子窗口。
- Chosen Solution: 记录了 `Start`、`TrayNotifyWnd`、`ReBarWindow32`、`MSTaskSwWClass`、`MSTaskListWClass`、`DesktopWindowContentBridge` 等节点。
- Rationale: 只有真实路径被记录下来，后续父窗口和锚点选择才站得住。
