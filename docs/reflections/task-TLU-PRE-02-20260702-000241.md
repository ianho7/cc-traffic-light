- Task: TLU-PRE-02 阅读 main.rs，定位当前 paint 路径
- Encountered Problem: 需要先确认当前 UI 逻辑究竟是“整块背景 + 文本”还是已经有更复杂的绘制分层。
- Thought Process: 重点检查 `paint_window`、`paint_style` 和 `WM_TIMER` 更新路径，判断是否能在不碰宿主逻辑时完成改造。
- Options Considered: 先看运行效果；先读代码路径。
- Chosen Solution: 先读 `main.rs`，直接锁定绘制函数和状态刷新函数。
- Rationale: 当前任务的主要风险在于改错层级，代码阅读比先跑 UI 更快收敛这一点。
