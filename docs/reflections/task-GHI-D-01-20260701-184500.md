- Task: GHI-D-01 build fixtures for empty, unrelated, and managed-old configs
- Encountered Problem: 不做 fixture 就只能直接拿真实用户配置试错，风险过高。
- Thought Process: 先覆盖三类最关键输入：空配置、已有无关 hooks、已有旧受管 marker。
- Options Considered: 只测空配置；只测真实 dry-run；建立三类最小 fixture。
- Chosen Solution: 在 `taskbar-widget/target/p1-hook-install-fixtures/` 下构造三类 JSON fixture。
- Rationale: 这足以验证新增、并存、更新三条主要 merge 路径。
