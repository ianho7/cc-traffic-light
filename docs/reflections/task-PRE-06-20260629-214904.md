- Task: PRE-06 阅读并摘录 Win11 单路径约束，不允许同时展开 Win10 或多显示器方案
- Encountered Problem: 环境字段有噪声，最容易诱发“顺手兼容一下 Win10”的范围膨胀。
- Thought Process: 在真正编码前就把单系统、单路径、单绘制方案写死到 preflight 文档里，减少后续临时起意扩范围的空间。
- Options Considered: 先写代码后再补边界；保留双路径可能性；立即锁定 Win11 单路径。
- Chosen Solution: 在 preflight 文档增加 `Scope Locks`，明确禁止 Win10、多显示器、GUI 框架和透明绘制并行展开。
- Rationale: 对这个 MVP 来说，边界约束和代码本身一样重要。
