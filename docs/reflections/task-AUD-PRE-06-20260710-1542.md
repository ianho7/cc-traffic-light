- Task: AUD-PRE-06 基础验证命令和构建顺序
- Encountered Problem: workspace 有明确的 host 验收约束，不能用 workspace-wide build 替代分包构建。
- Thought Process: 对照 AGENTS.md 和 pack-all.ps1，确认 settings → hook CLI → host 的顺序，并实际运行 SkipFrontend release pack。
- Options Considered: cargo build --workspace；直接构建 host；按 settings、hook CLI、host 分阶段构建。
- Chosen Solution: 采用分阶段构建，host 作为最后一个验收 artifact。
- Rationale: 遵守仓库约束，避免 Tauri features 回流 host dependency graph。
- Evidence: scripts/pack-all.ps1 -SkipFrontend 成功，三个 release artifact 均存在。
- Status: completed
