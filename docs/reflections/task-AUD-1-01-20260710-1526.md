- Task: AUD-1-01 显式构建 release taskbar_widget_hook.exe
- Encountered Problem: 原发布脚本只显式构建 settings 和 host，hook CLI 是否产出依赖 Cargo 默认行为。
- Thought Process: 发布 artifact 必须由脚本明确声明，否则 installer.iss 可能引用不存在或过期的文件。
- Options Considered: 继续依赖 host package 构建；在 pack-all.ps1 中增加独立 bin 构建；改为复制 debug artifact。
- Chosen Solution: 在 pack-all.ps1 中增加 cargo build -p taskbar-widget --bin taskbar_widget_hook --release --offline。
- Rationale: 保持稳定 release 路径，并让构建失败尽早暴露。
- Evidence: scripts/pack-all.ps1 -SkipFrontend 成功；target/release/taskbar_widget_hook.exe 已生成。
- Status: completed
