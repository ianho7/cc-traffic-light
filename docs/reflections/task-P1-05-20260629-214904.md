- Task: P1-05 用最小 `println!` 输出程序启动、窗口创建、进入消息循环等关键节点
- Encountered Problem: 后续进入任务栏探测和 `SetParent` 后，若没有早期日志，很难区分是窗口基线没起来还是嵌入逻辑出错。
- Thought Process: 在不引入日志框架的前提下，只保留最有诊断价值的少量事件。
- Options Considered: 不打日志；引入日志库；使用最小 `println!` 包装。
- Chosen Solution: 通过 `debug_log()` 输出 bootstrap、类注册、状态初始化、窗口显示和销毁等关键节点。
- Rationale: 这已经足够支撑 MVP 排错，不需要额外依赖。
