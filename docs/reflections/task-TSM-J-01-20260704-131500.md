- Task: TSM-J-01 更新架构文档，明确项目从“Win32 + Slint settings”演进为“Win32 host + Tauri settings + shared-core”
- Encountered Problem: 现有架构文档仍混有“未来会引入 shared-core”“默认入口还是 Slint”这类历史状态，已经落后于当前代码和验证结果。
- Thought Process: 我先用当前宿主入口、settings 进程管理、named pipe IPC 和 lifecycle 验证结果反推真实架构，再把文档中的“计划态”改成“已落地态”。
- Options Considered: 继续保留旧文档只追加备注；新建一份完全独立的迁移后架构文档；直接更新现有 baseline，让它成为当前事实源。
- Chosen Solution: 直接更新 `docs/plan/tauri-settings-architecture-baseline.md`，写清默认入口、shared-core 角色、进程边界、通信方向和已验证状态。
- Rationale: 这样能最小化文档分叉，让后续接手者只看一份 baseline 就能理解当前混合架构。
