- Task: VAL-03 探测日志中可见非空 `Shell_TrayWnd` 与当前选定 Win11 参考窗口信息
- Encountered Problem: 在错误桌面会话里跑同样的代码会得到空结果，因此需要明确哪份日志才是有效证据。
- Thought Process: 只接受提权交互桌面下的真实运行日志作为 `VAL-03` 证据。
- Options Considered: 接受沙箱日志；接受任意 FindWindow 输出；只接受交互桌面探测日志。
- Chosen Solution: 使用显示 `Shell_TrayWnd=0x10102` 和相关子窗口矩形的交互桌面日志。
- Rationale: 这才覆盖了真实需求范围。
