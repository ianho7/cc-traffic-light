- Task: AUD-4-05 state 损坏恢复
- Encountered Problem: state.json 损坏时既要保留原始证据，又要让新 hook 能恢复写入。
- Thought Process: 预先写入非法 JSON，调用 release hook CLI，再检查 corrupt backup、恢复后的 state 和 recovery timestamp。
- Options Considered: 直接覆盖损坏文件；让 widget 回落 idle；备份损坏文件并初始化新 state。
- Chosen Solution: 使用现有 backup_corrupt_state + default state recovery。
- Rationale: 保留诊断证据，同时恢复监控链路。
- Evidence: backup_count=1、task=working、recovery_at=true；另一次 atomic fixture state_exists=true、temp_count=0；临时目录已清理。
- Status: completed
