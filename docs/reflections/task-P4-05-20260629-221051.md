- Task: P4-05 已通过任务栏窗口捕获图确认任务栏内可见固定文本模块
- Encountered Problem: 日志坐标只能说明“理论上挪过去了”，不能单独证明任务栏里真的出现了 `TASKBAR WIDGET`。
- Thought Process: 先尝试普通屏幕截图，失败后改为直接对 `Shell_TrayWnd` 做 `PrintWindow` 捕获。
- Options Considered: 只依赖日志；继续用 `CopyFromScreen`；直接抓 `Shell_TrayWnd` 图像。
- Chosen Solution: 使用 `PrintWindow` 抓到任务栏图，并在图中看到右侧黑色模块与 `TASKBAR WIDGET` 文本。
- Rationale: 这是当前最强的 Phase 4 可见性证据。
