- Task: VAL-05 模块在任务栏内可见，显示固定文本，而非桌面浮窗
- Encountered Problem: 任务栏坐标正确不等于用户真的看得到模块内容。
- Thought Process: 必须把“嵌入”和“可见文字”同时证明出来。
- Options Considered: 只看 `SetParent`；只看 `MoveWindow`；看 `PrintWindow` 图像里的实际文字结果。
- Chosen Solution: 通过 `Shell_TrayWnd` 捕获图确认右侧任务栏中存在 `TASKBAR WIDGET` 文字模块。
- Rationale: 这直接满足了 MVP 的可见性要求。
