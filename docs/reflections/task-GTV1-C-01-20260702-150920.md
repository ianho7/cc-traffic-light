- Task: GTV1-C-01 Implement a fixed-priority detector skeleton for Codex
- Encountered Problem: Phase 2 already had UI surfaces reading a shared snapshot, but detector logic still lived implicitly inside ui_state mapping from the legacy hook state file.
- Thought Process: I separated source observation and aggregation into a dedicated detector module so Codex could have explicit source-priority slots even before all sources are implemented.
- Options Considered: Keep extending ui_state directly; add ad hoc detector code inside main.rs; create a detector module with explicit observation kinds and priority rules.
- Chosen Solution: Create a detector module with explicit observation kinds and priority rules.
- Rationale: Codex zero-config detection will need multiple evolving sources, and that logic should not be scattered across UI state structures.
