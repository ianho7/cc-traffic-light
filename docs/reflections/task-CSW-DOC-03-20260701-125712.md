- Task: CSW-DOC-03 document that dump script remains available after switching default hooks
- Encountered Problem: 项目默认 hooks 已切到真实状态写入，如果 README 不说明 dump 脚本仍保留，后续排障时容易误以为采样路径消失了。
- Thought Process: 在 `taskbar-widget/README.md` 的 Hook State Integration 段落追加一行，明确项目默认路径和 dump 脚本的保留用途。
- Options Considered: 只在 handoff 里提；只在 checklist 注记；同步写进 README。
- Chosen Solution: 在 README 中补充“项目级 `.codex/hooks.json` 已切到真实写入，dump 脚本仍保留用于重采样”。
- Rationale: README 是后续维护者最容易先读到的入口，放在这里比只写在 handoff 里更稳。
