- Task: CSW-VAL-07 verify that the summary changes across allowed real states
- Encountered Problem: 需要证明不仅单个 task 变化，聚合 summary 也确实随真实事件推进。
- Thought Process: 检查 list 输出中的 `global_summary` 和 `agents.codex.summary`，并对照各真实 task 的 `working` / `done`。
- Options Considered: 只看单个 task；只看 global_summary；同时看 task 层和 summary 层。
- Chosen Solution: 采用 task 层和 summary 层联合判断。
- Rationale: 这更符合 widget 只消费 `global_summary` 的实现事实。
