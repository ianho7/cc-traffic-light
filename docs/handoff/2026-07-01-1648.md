# Session Handoff

日期：2026-07-01 16:48

## 本轮新增证据

- 继续沿着 [2026-07-01-1345.md](/D:/project/cc-traffic-light/docs/handoff/2026-07-01-1345.md) 的最小补证路径，只验证一件事：
  - 真实 `%APPDATA%` 状态源下，widget 是否能在同一实例里把可见摘要从 `N` 更新到 `N+1`
- 这轮没有再补任何 hooks 配置或 schema 改动；只做了更短时序的真实运行期采样。

## 已再次确认的正证据

- 真实 hooks 仍然正常写入：
  - `taskbar_widget_hook.exe list` 一直能看到真实 `codex_<session_id>`
  - `session_id_source = payload`
  - `global_summary.active_task_count` 在真实线程事件后可从 `2 -> 3 -> 4`
- 两轮 fresh widget 启动后，真实状态源下的初始可见截图分别为：
  - [p0-redraw-real-live-20260701-164239/before-screen.png](/D:/project/cc-traffic-light/taskbar-widget/target/p0-redraw-real-live-20260701-164239/before-screen.png) 显示 `WAITING 2`
  - [p0-redraw-real-live-20260701-164536/before-screen.png](/D:/project/cc-traffic-light/taskbar-widget/target/p0-redraw-real-live-20260701-164536/before-screen.png) 显示 `WAITING 3`

## 新收紧的 blocker

- 在上述两轮里，我都在真实新线程事件后做了 2-3 秒内的 after 采样：
  - [p0-redraw-real-live-20260701-164239/after-screen.png](/D:/project/cc-traffic-light/taskbar-widget/target/p0-redraw-real-live-20260701-164239/after-screen.png)
  - [p0-redraw-real-live-20260701-164536/after-screen.png](/D:/project/cc-traffic-light/taskbar-widget/target/p0-redraw-real-live-20260701-164536/after-screen.png)
- 两张 after 图都为空白，不是旧值，也不是新的 `WAITING 3/4`。
- 与此同时，状态 verifier 明确显示真实状态已经推进：
  - 一轮到 `active_task_count = 3`
  - 下一轮到 `active_task_count = 4`
- 对应 `widget.pid.txt` 里的 PID，在 after 阶段再次查不到存活进程。

## 当前判断

- “真实 hooks -> 共享状态写入”已经足够通过，不再是 blocker。
- “widget 能在真实状态源下启动并显示当前 summary”也已经通过。
- 当前唯一剩余 blocker 已经收紧为：
  - widget 在尝试验证 live redraw 的 after 阶段反复退出或不可见
  - 因而拿不到同一实例上的稳定 `before -> after` 可见刷新证据

## 下一步建议

如果继续，方向不该再是重复触发线程，而应该直接诊断 widget 生命周期：

1. 给 `taskbar-widget.exe` 的真实状态路径增加更强的运行期日志，至少落盘：
   - 轮询 tick
   - 读取状态成功/失败
   - summary 变化
   - 退出前最后一条日志
2. 查明为什么 fresh 启动能显示 `WAITING 2/3`，但 2-3 秒后的同实例采样会变成空白。
3. 在确认实例稳定存活前，不要再把“after 为空”解释成 redraw 本身失败。
