# Session Handoff

日期：2026-07-01 20:53

## 本轮完成的验证

- Claude Code hooks 的真实 payload shape 已完成首批采样：
  - `UserPromptSubmit`
  - `PreToolUse`
  - `PostToolUse`
  - `Notification`
  - `PostToolUseFailure`
  - `Stop`
- 项目本地 Claude hooks 配置已切换到真实状态写入：
  - [settings.local.json](/D:/project/cc-traffic-light/.claude/settings.local.json)
- 真实 Claude 会话已成功写入共享状态文件。

## 新证据

- 真实 Claude sample 已确认：
  - `hook_event_name` 存在
  - `session_id` 存在
  - `turn_id` 未观察到
  - `event_order` 未观察到
  - 当前继续使用 `received_at` 兜底排序
- 真实状态写入结果：
  - `taskbar_widget_hook.exe list` 输出中已出现：
    - `claude_f17c5d80-f65f-4fbf-9122-200e40d13590`
  - 关键字段：
    - `agent = claude`
    - `session_id_source = payload`
    - `state = done`
    - `hook_name = Stop`
    - `summary_eligible = true`
- Claude 与 Codex 已在同一状态文件中并存：
  - `agents.claude.summary.state = done`
  - `agents.claude.summary.active_task_count = 1`
  - `agents.codex.summary.state = waiting`
  - `global_summary` 继续由更高优先级的 waiting task 主导，没有被 Claude 记录污染

## 实现修正

- Claude hooks 真实写入阶段踩到两个配置问题，已收敛：
  - `settings.local.json` 一度仍指向 `sample-hook.ps1`
  - Claude 这版执行 command 时需要显式 `shell: "powershell"`，否则会把 Windows 路径交给 `/usr/bin/bash`
- 当前有效配置做法是：
  - 每个 hook 项都设置 `shell: "powershell"`
  - `command` 采用：
    - `& 'D:\project\cc-traffic-light\taskbar-widget\target\debug\taskbar_widget_hook.exe' claude <HookName>`

## 当前结论

- P2 的核心目标已经达到：
  - 真实 Claude payload 已不再是猜测
  - `session_id` / `hook_event_name` 已有真实证据
  - 真实 Claude 会话可写入与 Codex 相同的共享状态模型
- 当前剩余工作不是“能不能接入”，而是“是否还要把 fallback/边界场景验证补齐到更高证据等级”。

## 仍未清零的点

- 没有专门构造“缺失 session_id”场景来验证 `claude_unknown` / `summary_eligible=false` 的 fallback 防护。
- 没有在真实 `state.json` 中分别留存 `working` / `waiting` 的 Claude 最终证据，只确认了 `done` 路径和 shape-only 事件语义。
- `examples.claude-hooks.json` 还可以进一步标注“哪些事件已验证、哪些只是当前推荐集合”。

## 下一步建议

1. 如果目标是关闭 P2，可补最少量文档收尾：
   - 标注 `examples.claude-hooks.json` 中已验证事件
   - 清理 checklist 剩余非关键项
2. 如果目标是继续 hardening：
   - 先补 `CCH-D-05` / `CCH-E-04` 的 fallback 验证
   - 再视需要进入 P3/P4，把 Claude 的真实 `working` / `waiting` 记录也补成直接证据
